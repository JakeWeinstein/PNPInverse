---
phase: 14-post-cleanup-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "scripts/**/*.py"
  - "tests/**/*.py"
  - "Forward/**/*.py"
  - "Inverse/**/*.py"
  - "FluxCurve/**/*.py"
  - "Nondim/**/*.py"
  - "Surrogate/**/*.py"
autonomous: false
requirements: [VRFY-01, VRFY-02]
must_haves:
  truths:
    - "All v13 pipeline scripts import without errors"
    - "All library modules (Forward, Inverse, FluxCurve, Nondim, Surrogate) import without errors"
    - "All 16 kept test files import without errors"
    - "The full pytest suite passes with no import-related failures"
    - "Any non-import test failures are documented for follow-up"
  artifacts:
    - path: "scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py"
      provides: "v13 master script imports cleanly"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures import cleanly"
  key_links:
    - from: "scripts/"
      to: "Forward, Inverse, FluxCurve, Nondim, Surrogate"
      via: "Python imports"
      pattern: "^(from|import) (Forward|Inverse|FluxCurve|Nondim|Surrogate)"
    - from: "tests/"
      to: "Forward, Inverse, FluxCurve, Nondim, Surrogate, scripts"
      via: "Python imports"
      pattern: "^(from|import) (Forward|Inverse|FluxCurve|Nondim|Surrogate|scripts)"
---

<objective>
Verify that the v13 pipeline, all remaining scripts, library modules, and the kept pytest test suite work correctly after phases 12-13 deleted ~84 files and archived ~108 directories. Fix broken imports; report deeper test failures.

Purpose: Confirm the repo is in a clean, working state before closing the v1.1 milestone.
Output: All imports verified, pytest suite run, any issues fixed or documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-post-cleanup-verification/14-CONTEXT.md
@.planning/phases/13-delete-dead-code/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static and runtime import verification for all kept Python files</name>
  <files>
    scripts/_bv_common.py
    scripts/studies/profile_likelihood_pde.py
    scripts/studies/profile_likelihood_study.py
    scripts/studies/run_multi_seed_v13.py
    scripts/studies/sensitivity_visualization.py
    scripts/surrogate/build_surrogate.py
    scripts/surrogate/generate_training_data.py
    scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py
    scripts/surrogate/multistart_inference.py
    scripts/surrogate/overnight_train_v11.py
    scripts/surrogate/train_improved_surrogate.py
    scripts/surrogate/train_nn_surrogate.py
    scripts/surrogate/validate_surrogate.py
    scripts/verification/mms_bv_4species.py
    scripts/verification/test_bv_forward.py
    Forward/__init__.py
    Inverse/__init__.py
    FluxCurve/__init__.py
    Nondim/__init__.py
    Surrogate/__init__.py
    tests/conftest.py
  </files>
  <action>
Two-pass import verification per user decision:

**Pass 1 -- Static analysis (AST parse):**
Write a small Python script that uses `ast.parse()` + `ast.walk()` to extract all `import X` and `from X import Y` statements from every kept .py file. Cross-reference against the file tree to flag any imports that reference deleted modules or packages. Specifically check for:
- References to deleted scripts (scripts/inference/, scripts/bv/, old v8-v12 surrogate scripts)
- References to deleted test modules (test_bcd, test_cascade, test_cascade_pde_hybrid, etc.)
- References to deleted src modules (if any)
Print a report of all broken import references found.

**Pass 2 -- Runtime import check:**
Activate `../venv-firedrake` virtualenv and attempt to import each kept module/script. For scripts, use `python -c "import ast; ast.parse(open('script.py').read())"` for syntax check, then `python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'script.py'); mod = importlib.util.module_from_spec(spec)"` to verify the module can be loaded (catching ImportError).

For library packages, run:
```
python -c "import Forward; import Inverse; import FluxCurve; import Nondim; import Surrogate"
```

**Fix broken imports:**
- If an import references a deleted module, remove or update the import statement
- If a test file primarily tests deleted code, delete the entire test file
- Document each fix made

**Do NOT fix non-import issues** (runtime errors, test logic failures) -- only document them.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && source ../venv-firedrake/bin/activate && python -c "import Forward; import Inverse; import FluxCurve; import Nondim; import Surrogate; print('All library packages import OK')"</automated>
  </verify>
  <done>
    - All 5 library packages (Forward, Inverse, FluxCurve, Nondim, Surrogate) import without error
    - All 15 scripts in scripts/ have no broken import references
    - All 16 test files in tests/ have no broken import references
    - Any import fixes applied are documented with rationale
    - Any broken imports in test files that primarily test deleted code result in those test files being deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full pytest suite and document results</name>
  <files>tests/conftest.py, tests/test_bv_forward.py, tests/test_diagnostic_metadata.py, tests/test_inference_config.py, tests/test_inverse_verification.py, tests/test_mms_convergence.py, tests/test_multi_seed_aggregation.py, tests/test_multistart.py, tests/test_nondim.py, tests/test_params.py, tests/test_pipeline_reproducibility.py, tests/test_profile_likelihood.py, tests/test_sensitivity_visualization.py, tests/test_steady_state_common.py, tests/test_surrogate_fidelity.py, tests/test_v13_verification.py</files>
  <action>
Activate `../venv-firedrake` virtualenv and run the full pytest suite with no timeout. MMS convergence and PDE tests are expected to take several minutes -- let them finish.

Run command:
```bash
source ../venv-firedrake/bin/activate
cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse
pytest tests/ -v --tb=short 2>&1
```

Analyze results:
1. If all tests pass: record the pass count and confirm VRFY-02
2. If tests fail with import errors: go back and fix (these should have been caught in Task 1)
3. If tests fail with non-import errors: document each failure with test name, error type, and brief diagnosis. Do NOT fix these -- report them for follow-up per user decision.

Any Firedrake test failures are real issues to report (env will be properly activated).
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && source ../venv-firedrake/bin/activate && pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pytest suite runs to completion (no hangs, no crashes)
    - All tests pass OR any failures are documented with diagnosis
    - Import-related failures have been fixed (loop back to Task 1 if needed)
    - Non-import failures documented for follow-up without attempting fixes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of post-cleanup results</name>
  <action>
    Present import verification report and pytest results for human review.
  </action>
  <what-built>
    Import verification and full pytest suite run across the entire post-cleanup codebase. All broken imports fixed, test results documented.
  </what-built>
  <how-to-verify>
    1. Review the import verification report -- confirm no remaining broken imports
    2. Review the pytest results -- confirm pass/fail counts match expectations
    3. If any non-import test failures were reported, review the documented diagnoses
    4. Confirm the repo is in a clean working state for closing v1.1 milestone
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "import Forward; import Inverse; import FluxCurve; import Nondim; import Surrogate"` succeeds
2. `pytest tests/ -v --tb=short` runs to completion
3. No import-related test failures remain
4. Any non-import failures are documented
</verification>

<success_criteria>
- All v13 pipeline imports resolve correctly (VRFY-01)
- Kept pytest test suite passes with no import failures (VRFY-02)
- Non-import test failures documented for follow-up
- Repo confirmed clean and working for v1.1 milestone closure
</success_criteria>

<output>
After completion, create `.planning/phases/14-post-cleanup-verification/14-01-SUMMARY.md`
</output>
