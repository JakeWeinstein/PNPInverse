---
phase: 08-ablation-audit
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - scripts/studies/analyze_ablation_v14.py
  - tests/test_ablation_analysis.py
autonomous: true
requirements: [AUDT-01, AUDT-02, AUDT-03, AUDT-04]

must_haves:
  truths:
    - "Wilcoxon signed-rank test determines per-parameter redundancy/justification for each ablation config vs baseline"
    - "Seed alignment uses set intersection (not row index) to handle missing Phase 7 seeds"
    - "Every v13 stage has a justification status entry (justified/redundant/unjustified/N/A) with evidence"
    - "Minimal pipeline spec lists only survived stages with per-stage timing data"
    - "Box plot comparison figure shows all 4 configs side-by-side per parameter"
  artifacts:
    - path: "scripts/studies/analyze_ablation_v14.py"
      provides: "Wilcoxon analysis, justification table, minimal pipeline spec, box plots"
      min_lines: 200
    - path: "tests/test_ablation_analysis.py"
      provides: "Unit tests for Wilcoxon logic, seed alignment, justification table, spec format"
      min_lines: 100
    - path: "StudyResults/v14/ablation/wilcoxon_results.csv"
      provides: "Per-config per-parameter p-values and verdicts"
      contains: "p_value"
    - path: "StudyResults/v14/ablation/justification_table.md"
      provides: "Stage justification status table"
      contains: "justified"
    - path: "StudyResults/v14/ablation/minimal_pipeline_spec.md"
      provides: "Minimal pipeline specification for Phase 9 and Phase 11"
      contains: "survived"
  key_links:
    - from: "scripts/studies/analyze_ablation_v14.py"
      to: "StudyResults/v14/ablation/config**/seed_results.csv"
      via: "CSV loading with seed-keyed dict for pairing"
      pattern: "load_seed_results"
    - from: "scripts/studies/analyze_ablation_v14.py"
      to: "StudyResults/v14/multi_seed/seed_results.csv"
      via: "Loading Phase 7 baseline as config 1"
      pattern: "baseline.*seed_results"
    - from: "scripts/studies/analyze_ablation_v14.py"
      to: "scipy.stats.wilcoxon"
      via: "Paired signed-rank test with alternative='greater'"
      pattern: "wilcoxon.*alternative.*greater"
---

<objective>
Analyze ablation results using paired Wilcoxon signed-rank tests, produce a stage justification table, generate the minimal pipeline specification, and create comparison visualizations.

Purpose: Transform raw ablation data into actionable decisions about which v13 pipeline stages are justified, redundant, or unjustified -- feeding directly into Phase 9 experiments and Phase 11 pipeline build.
Output: `wilcoxon_results.csv`, `justification_table.md`, `minimal_pipeline_spec.md`, comparison box plots, and AUDT-04 metadata.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ablation-audit/08-CONTEXT.md
@.planning/phases/08-ablation-audit/08-RESEARCH.md
@.planning/phases/08-ablation-audit/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 08-01: ablation runner outputs consumed by this plan -->

Expected directory structure after Plan 08-01:
```
StudyResults/v14/ablation/
  config2_s4only_p1p2/seed_results.csv
  config3_s4only_p2/seed_results.csv
  config4_coldstart_p1p2/seed_results.csv
```

Each seed_results.csv has columns:
  seed, k0_1, k0_2, alpha_1, alpha_2, k0_1_err_pct, k0_2_err_pct, alpha_1_err_pct, alpha_2_err_pct

Phase 7 baseline at:
  StudyResults/v14/multi_seed/seed_results.csv (same schema, 17 seeds -- missing 3, 7, 9)

From Plan 08-01 parse_all_phase_rows():
  Returns list of dicts with keys: phase, k0_1, k0_2, alpha_1, alpha_2, time_s
  Available per config for timing analysis.

Wilcoxon API (from RESEARCH.md):
```python
from scipy.stats import wilcoxon
stat, p_value = wilcoxon(ablated_errors, baseline_errors, alternative='greater')
# p >= 0.05 -> stage is "redundant" (removal doesn't significantly worsen)
# p < 0.05  -> stage is "justified" (removal significantly hurts)
```

Justification table format (CONTEXT.md locked):
  Stage | Status (justified/redundant/unjustified) | Criterion (literature/empirical/simplest) | Evidence summary
  S4 gets literature credit; S1-S3 need empirical; S5 is "N/A: selection logic"; P1/P2 by ablation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create analysis tests and implement Wilcoxon comparison logic</name>
  <files>tests/test_ablation_analysis.py, scripts/studies/analyze_ablation_v14.py</files>
  <behavior>
    - TestSeedAlignment: load_seed_results returns dict keyed by seed integer, not row index
    - TestSeedAlignment: common seeds computed via set intersection handles missing seeds correctly (e.g., baseline has 17 seeds, ablated has 20 -> 17 common)
    - TestSeedAlignment: empty intersection raises ValueError with clear message
    - TestWilcoxonComparison: run_wilcoxon_comparison returns dict with keys: param, n_pairs, stat, p_value, verdict, median_baseline, median_ablated, max_baseline, max_ablated
    - TestWilcoxonComparison: verdict is "redundant" when p >= 0.05 (ablation does NOT significantly worsen)
    - TestWilcoxonComparison: verdict is "justified" when p < 0.05 (ablation significantly worsens)
    - TestWilcoxonComparison: uses alternative='greater' (one-sided test: ablated > baseline means stage helped)
    - TestP1Contribution: config3 vs config2 comparison isolates P1 contribution specifically
    - TestJustificationTable: write_justification_table produces markdown with all required columns (Stage, Status, Criterion, Evidence)
    - TestJustificationTable: all 7 stage entries present (S1-S5, P1, P2) with correct criterion assignments
    - TestJustificationTable: S4 gets "literature" criterion, S5 gets "N/A", P2 gets "simplest"
    - TestMinimalPipelineSpec: write_minimal_pipeline_spec includes only stages with "justified" status
    - TestMinimalPipelineSpec: spec includes timing data section with median wall-clock per stage
    - TestAudt04Metadata: metadata.json has all required AUDT-04 keys
  </behavior>
  <action>
    RED: Write comprehensive tests in tests/test_ablation_analysis.py. Use synthetic error arrays for Wilcoxon tests -- construct arrays where one is clearly worse (p < 0.05) and arrays where they are similar (p >= 0.05) to test both verdict paths.

    GREEN: Implement scripts/studies/analyze_ablation_v14.py:

    1. load_seed_results(csv_path) -> dict[int, dict]: Load seed_results.csv as {seed_int: {param: value}}. Key by seed column value, NOT row index.

    2. get_common_seeds(baseline_dict, ablated_dict) -> list[int]: Return sorted set intersection. Raise ValueError if empty.

    3. run_wilcoxon_comparison(baseline_errors, ablated_errors, param_name) -> dict: Paired Wilcoxon signed-rank with alternative='greater'. Return stat, p_value, verdict, median/max descriptive stats.

    4. compare_all_configs(baseline_path, config_paths) -> list[dict]: For each config, for each parameter (k0_1, k0_2, alpha_1, alpha_2), run Wilcoxon test. Output: flat list of result dicts.

    5. determine_stage_status(wilcoxon_results) -> dict: Map Wilcoxon results to stage justification status:
       - S1-S3 (block): status from config2 vs config1 comparison (config2 removes S1-S3 cascade/joint)
       - S4: "justified" with criterion "literature" (locked in CONTEXT.md)
       - S5: "N/A: selection logic" (locked in CONTEXT.md)
       - P1: status from config3 vs config2 comparison (config3 removes P1)
       - P2: "justified" with criterion "simplest" (always required)

    6. write_justification_table(stage_statuses, output_path): Write markdown table with columns: Stage | Status | Criterion | Evidence summary. Add 1-2 sentence narrative per stage.

    7. write_minimal_pipeline_spec(stage_statuses, timing_data, output_path): Write markdown spec listing only survived stages. Include per-stage median timing from parse_all_phase_rows output. State what survived -- no Phase 9 recommendations per CONTEXT.md.

    8. write_wilcoxon_csv(results, output_path): Write wilcoxon_results.csv with all comparison results.

    9. plot_ablation_comparison(config_results, output_path): 4-panel box plot (one per parameter, one box per config). Follow Phase 7 matplotlib pattern (Agg backend, savefig, plt.close).

    10. write_audt04_metadata(output_path): AUDT-04 sidecar with required keys.

    CLI: --baseline-dir (default StudyResults/v14/multi_seed), --ablation-dir (default StudyResults/v14/ablation), --output-dir (default StudyResults/v14/ablation)

    CRITICAL: Use alternative='greater' for Wilcoxon (NOT default two-sided). See Pitfall 2 in RESEARCH.md.
    CRITICAL: Align seeds by value, not row index. Phase 7 baseline has 17 seeds (missing 3, 7, 9). See Pitfall 1.
    CRITICAL: S1-S3 are assessed as a single block per CONTEXT.md locked decision.
    CRITICAL: Timing data comes from parse_all_phase_rows output (Plan 08-01), aggregated across seeds. Parse time_s column from the per-config CSVs.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_ablation_analysis.py -x -v</automated>
  </verify>
  <done>
    - Wilcoxon comparison correctly classifies redundant (p >= 0.05) and justified (p < 0.05) using alternative='greater'
    - Seed alignment uses set intersection, not row index
    - Justification table has all 7 stages with correct criterion assignments
    - Minimal pipeline spec includes only justified stages with timing data
    - Box plot comparison figure generated
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse
python -m pytest tests/test_ablation_analysis.py tests/test_ablation_runner.py -x -v
python -c "from scripts.studies.analyze_ablation_v14 import run_wilcoxon_comparison, determine_stage_status; print('Analysis module importable')"
```
</verification>

<success_criteria>
- analyze_ablation_v14.py exists with CLI and all 10 functions
- Wilcoxon tests use alternative='greater' and correct verdict logic
- Seed alignment by value (set intersection), not row index
- wilcoxon_results.csv written with per-config per-parameter p-values
- justification_table.md has all 7 stages with Status/Criterion/Evidence columns
- minimal_pipeline_spec.md lists only survived stages with timing section
- Box plot comparison figure saved
- AUDT-04 metadata.json sidecar written
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-ablation-audit/08-02-SUMMARY.md`
</output>
