---
phase: 05-pipeline-reproducibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_pipeline_reproducibility.py
  - StudyResults/pipeline_reproducibility/regression_baselines.json
autonomous: true
requirements: [PIP-01, PIP-02]

must_haves:
  truths:
    - "Running pytest with --update-baselines generates surrogate-only baselines in StudyResults/pipeline_reproducibility/regression_baselines.json"
    - "Running pytest without --update-baselines compares surrogate-only outputs against saved baselines at rel=1e-10"
    - "Test fails with clear message if baselines do not exist and --update-baselines not passed"
    - "Test failure prints a diff table showing parameter name, baseline, current, abs diff, rel diff, tolerance, pass/fail"
    - "Surrogate-only test runs without Firedrake (no @pytest.mark.slow)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "--update-baselines pytest option and fixture"
      contains: "update-baselines"
    - path: "tests/test_pipeline_reproducibility.py"
      provides: "TestSurrogateReproducibility class with baseline load/save/compare"
      min_lines: 100
    - path: "StudyResults/pipeline_reproducibility/regression_baselines.json"
      provides: "Saved surrogate-only baseline values with metadata"
      contains: "surrogate_only"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_pipeline_reproducibility.py"
      via: "update_baselines fixture injection"
      pattern: "update_baselines"
    - from: "tests/test_pipeline_reproducibility.py"
      to: "StudyResults/pipeline_reproducibility/regression_baselines.json"
      via: "json load/save of baselines"
      pattern: "regression_baselines\\.json"
    - from: "tests/test_pipeline_reproducibility.py"
      to: "scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py"
      via: "import _run_surrogate_phases for S1+S2"
      pattern: "_run_surrogate_phases"
---

<objective>
Create the conftest --update-baselines plugin, baseline load/save/diff helpers, and the fast surrogate-only reproducibility test (TestSurrogateReproducibility). This test imports _run_surrogate_phases() directly, runs S1+S2 with fixed seeds, and compares inferred parameters + loss against saved JSON baselines at rel=1e-10.

Purpose: Establishes the regression baseline infrastructure (PIP-02) and proves surrogate-only inference is deterministic (PIP-01) without requiring Firedrake.
Output: Working fast test + generated baselines JSON + conftest plugin.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-reproducibility/05-CONTEXT.md
@.planning/phases/05-pipeline-reproducibility/05-RESEARCH.md
@tests/conftest.py
@scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py
@scripts/_bv_common.py
@Surrogate/objectives.py
@Surrogate/multistart.py

<interfaces>
<!-- Key integration points the executor needs -->

From tests/conftest.py:
```python
FIREDRAKE_AVAILABLE = _firedrake_available()
skip_without_firedrake = pytest.mark.skipif(...)
# No existing pytest_addoption -- executor must add one
```

From scripts/_bv_common.py (true parameters):
```python
K0_HAT_R1, K0_HAT_R2, ALPHA_R1, ALPHA_R2  # True inference targets
setup_firedrake_env()  # Env vars only, safe to call (no Firedrake import)
```

From scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py:
```python
_run_surrogate_phases(surrogate, model_label, args, target_cd_surr, target_pc_surr,
                      target_cd_full, target_pc_full, all_eta, eta_shallow,
                      initial_k0_guess, initial_alpha_guess,
                      true_k0_arr, true_alpha_arr, secondary_weight) -> dict
# Returns dict with s1_alpha, surr_best_k0, surr_best_alpha, surr_best_loss, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --update-baselines conftest plugin and baseline helpers</name>
  <files>tests/conftest.py, tests/test_pipeline_reproducibility.py</files>
  <action>
1. In tests/conftest.py, add a `pytest_addoption` function that registers `--update-baselines` (action="store_true", default=False, help="Regenerate regression baselines instead of comparing against them"). Add an `update_baselines` fixture that returns `request.config.getoption("--update-baselines")`.

2. Create tests/test_pipeline_reproducibility.py with:
   - Module docstring explaining purpose (PIP-01, PIP-02)
   - Constants: `_ROOT`, `_BASELINES_PATH = os.path.join(_ROOT, "StudyResults", "pipeline_reproducibility", "regression_baselines.json")`
   - `_load_baselines(path) -> dict | None`: Load JSON, return None if file missing
   - `_save_baselines(path, data)`: Save JSON with metadata block (generated_at ISO timestamp, git_commit via subprocess, python_version, numpy_version). Use `os.makedirs` for parent dir. Use `default=lambda o: o.tolist() if hasattr(o, 'tolist') else o` for numpy serialization.
   - `_format_diff_table(entries: list[tuple[str, float, float, float]]) -> tuple[str, bool]`: Format comparison table with columns: Parameter, Baseline, Current, Abs Diff, Rel Diff, Tol, Pass/Fail. Return (formatted_string, any_fail). Use the exact format from RESEARCH.md Pattern 5.
   - `_assert_baselines(current: dict, section: str, tolerance: float, baselines: dict, update_baselines: bool, baselines_path: str)`: If update_baselines, merge current into baselines[section] and save. If baselines is None, pytest.fail with "Baselines not found. Run with --update-baselines to generate." Otherwise, compare current values against baselines[section] using _format_diff_table, assert no failures with diff table in error message.

Follow existing test file conventions: `# ===` separators between sections, docstrings on all public helpers.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -c "from tests.test_pipeline_reproducibility import _load_baselines, _save_baselines, _format_diff_table; print('imports ok')"</automated>
  </verify>
  <done>conftest.py has --update-baselines option and fixture; test_pipeline_reproducibility.py has baseline load/save/diff helpers that import cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Implement TestSurrogateReproducibility and generate baselines</name>
  <files>tests/test_pipeline_reproducibility.py, StudyResults/pipeline_reproducibility/regression_baselines.json</files>
  <action>
In tests/test_pipeline_reproducibility.py, add class TestSurrogateReproducibility (NO @pytest.mark.slow, NO @skip_without_firedrake -- this is the fast test):

1. Module-scoped fixture `nn_ensemble_repro`: Load the D3-deeper NN ensemble (same pattern as test_inverse_verification.py). Skip if ensemble dir not found.

2. Module-scoped fixture `surrogate_only_results(nn_ensemble_repro)`:
   - Import `_run_surrogate_phases` from `scripts.surrogate.Infer_BVMaster_charged_v13_ultimate`
   - Import true params from `scripts._bv_common` (K0_HAT_R1, K0_HAT_R2, ALPHA_R1, ALPHA_R2)
   - Generate surrogate targets at true params (call nn_ensemble_repro.predict with true k0/alpha values)
   - Build a SimpleNamespace args object with surr_strategy="joint" and other required attributes (inspect the v13 script to determine all needed attributes)
   - Call `_run_surrogate_phases()` with fixed seeds (seed=42 in MultiStartConfig), initial guesses [0.005, 0.0005] for k0 and [0.4, 0.3] for alpha
   - Return the result dict

3. TestSurrogateReproducibility class with test methods:
   - `test_surrogate_parameters_reproducible(self, surrogate_only_results, update_baselines)`: Extract s1_alpha, surr_best_k0, surr_best_alpha from results. Build current dict with these values. Call _assert_baselines with section="surrogate_only", tolerance=1e-10.
   - `test_surrogate_loss_reproducible(self, surrogate_only_results, update_baselines)`: Extract surr_best_loss. Call _assert_baselines with section="surrogate_only_loss", tolerance=1e-10.

4. After implementing, run `pytest tests/test_pipeline_reproducibility.py --update-baselines -x -v` to generate the initial baselines JSON.

5. Then run `pytest tests/test_pipeline_reproducibility.py -x -v` (without --update-baselines) to confirm it passes by comparing against the just-generated baselines.

Key details:
- The v13 script's _run_surrogate_phases returns keys like "s1_alpha", "surr_best_k0", "surr_best_alpha", "surr_best_loss" -- inspect the actual function to confirm exact key names.
- Surrogate-only (S1+S2) outputs are float64 deterministic, so rel=1e-10 is correct.
- Do NOT import Firedrake anywhere in this test path.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && pytest tests/test_pipeline_reproducibility.py -x -v -m "not slow" 2>&1 | tail -20</automated>
  </verify>
  <done>TestSurrogateReproducibility passes against generated baselines at rel=1e-10; baselines JSON exists in StudyResults/pipeline_reproducibility/ with metadata</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_pipeline_reproducibility.py -x -v -m "not slow"` -- surrogate-only tests pass
2. `pytest tests/test_pipeline_reproducibility.py --update-baselines -x` -- baselines regenerated without error
3. Baselines JSON contains "metadata" with git_commit, timestamps, versions
4. Baselines JSON contains "surrogate_only" section with parameter values
5. Running without baselines file fails with clear "Run with --update-baselines" message
</verification>

<success_criteria>
- TestSurrogateReproducibility runs in <30s without Firedrake
- Surrogate-only parameters match baselines at rel=1e-10
- --update-baselines flag works to generate and regenerate baselines
- Missing baselines produce clear error message
- Diff table appears on any tolerance failure
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-reproducibility/05-01-SUMMARY.md`
</output>
