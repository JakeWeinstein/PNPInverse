---
phase: 04-inverse-problem-verification
plan: 03
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - tests/test_inverse_verification.py
autonomous: false
requirements: [INV-01, INV-03]
gap_closure: true

must_haves:
  truths:
    - "TestParameterRecovery generates target I-V curves via PDE forward solves, not surrogate model.predict()"
    - "TestMultistartBasin uses PDE-generated targets for the recovery objective, not surrogate-generated targets"
    - "The nn_ensemble fixture still provides surrogate-generated targets for TestSurrogateFDConvergence (which legitimately tests surrogate self-consistency)"
    - "Slow tests pass in Firedrake environment and produce runtime artifacts"
  artifacts:
    - path: "tests/test_inverse_verification.py"
      provides: "Updated test file with PDE targets for INV-01 and INV-03"
      contains: "_pde_cd_at_params"
    - path: "StudyResults/inverse_verification/parameter_recovery_summary.json"
      provides: "Recovery error statistics from PDE-target-based tests"
    - path: "StudyResults/inverse_verification/multistart_basin.json"
      provides: "Multistart convergence statistics from PDE-target-based tests"
  key_links:
    - from: "tests/test_inverse_verification.py::TestParameterRecovery"
      to: "FluxCurve/bv_point_solve.py"
      via: "solve_bv_curve_points_with_warmstart for PDE target generation"
      pattern: "_pde_cd_at_params|solve_bv_curve_points_with_warmstart"
    - from: "tests/test_inverse_verification.py::TestMultistartBasin"
      to: "FluxCurve/bv_point_solve.py"
      via: "PDE-generated target_cd passed to run_multistart_inference"
      pattern: "pde_target|_pde_cd_at_params"
---

<objective>
Fix inverse crime in TestParameterRecovery (INV-01) and TestMultistartBasin (INV-03) by replacing surrogate-generated targets with PDE-generated targets, then have user run slow tests to confirm pass/fail.

Purpose: The verification report identified that both tests use `model.predict()` to generate targets, then recover parameters using the same surrogate -- a surrogate-on-surrogate inverse crime. The ROADMAP requires "synthetic v13 data" (PDE-generated) and the RESEARCH section explicitly warned against this pattern. TestGradientConsistencyPDE already has a `_pde_cd_at_params()` helper that generates PDE targets -- this pattern should be extracted and reused.
Output: Updated test_inverse_verification.py where INV-01 and INV-03 use PDE-generated targets. Confirmed test pass via user execution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inverse-problem-verification/04-CONTEXT.md
@.planning/phases/04-inverse-problem-verification/04-RESEARCH.md
@.planning/phases/04-inverse-problem-verification/04-VERIFICATION.md
@.planning/phases/04-inverse-problem-verification/04-02-SUMMARY.md
@tests/test_inverse_verification.py

<interfaces>
<!-- Key contracts for this gap closure -->

From tests/test_inverse_verification.py (existing PDE target generation in TestGradientConsistencyPDE):
```python
# Lines 506-561: _pde_cd_at_params() generates PDE current density
# Uses: setup_firedrake_env(), SteadyStateConfig, make_graded_rectangle_mesh,
#   solve_bv_curve_points_with_warmstart, _clear_caches,
#   make_bv_solver_params, make_recovery_config
# Returns: np.array of current_density values at phi_applied voltages
```

From tests/test_inverse_verification.py (nn_ensemble fixture, line 99-102):
```python
# PROBLEM: generates targets from surrogate
pred = model.predict(K0_HAT_R1, K0_HAT_R2, ALPHA_R1, ALPHA_R2)
target_cd = pred["current_density"]
target_pc = pred["peroxide_current"]
```

From tests/test_inverse_verification.py (TestParameterRecovery, lines 325-326):
```python
# PROBLEM: uses surrogate targets from nn_ensemble fixture
target_cd_clean = nn_ensemble["target_cd"].copy()
target_pc_clean = nn_ensemble["target_pc"].copy()
```

From tests/test_inverse_verification.py (TestMultistartBasin, lines 672-674):
```python
# PROBLEM: uses surrogate targets from nn_ensemble fixture
target_cd = nn_ensemble["target_cd"]
target_pc = nn_ensemble["target_pc"]
```

From scripts/_bv_common.py:
```python
K0_HAT_R1, K0_HAT_R2, ALPHA_R1, ALPHA_R2  # true parameter values
I_SCALE  # observable scale for current density
make_bv_solver_params()  # solver parameter builder
make_recovery_config()  # recovery config
FOUR_SPECIES_CHARGED, SNES_OPTS_CHARGED  # species and solver options
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace surrogate targets with PDE targets in TestParameterRecovery and TestMultistartBasin</name>
  <files>tests/test_inverse_verification.py</files>
  <action>
Refactor `tests/test_inverse_verification.py` to eliminate the inverse crime in INV-01 and INV-03:

1. **Extract `_pde_cd_at_params()` into a module-level helper function** (or a separate fixture). Currently this function is defined inside `TestGradientConsistencyPDE.test_pde_adjoint_vs_fd()` (lines 532-561). Extract it to module scope so all three PDE-dependent test classes can reuse it. The function needs:
   - Firedrake imports: `setup_firedrake_env()`, `SteadyStateConfig`, `make_graded_rectangle_mesh`, `solve_bv_curve_points_with_warmstart`, `_clear_caches`
   - Parameters: k0_1, k0_2, alpha_1, alpha_2
   - A voltage grid (phi_applied). Use the production v13 voltage grid from `_bv_common` if available, otherwise the existing 5-point grid `[-2.0, -5.0, -8.0, -12.0, -16.0]`. For TestParameterRecovery, a denser grid is better to match the surrogate's n_eta voltage points. Use `model.eta_values` or the existing `phi_applied` from the v13 script to get the actual production voltage grid.
   - Returns: np.array of current_density values

2. **Add a module-scoped `pde_targets` fixture** (or class-scoped fixture within TestParameterRecovery):
   - Marked with `@skip_without_firedrake` (it needs Firedrake to run)
   - Calls `_pde_cd_at_params(K0_HAT_R1, K0_HAT_R2, ALPHA_R1, ALPHA_R2)` at the production voltage grid
   - Returns dict with `target_cd` (PDE-generated current density) and the voltage grid used
   - NOTE: TestParameterRecovery currently uses `nn_ensemble["target_cd"]` for surrogate recovery and also needs to construct a SurrogateObjective with target_cd as the target. The key insight is that the TARGET data should come from PDE, but the RECOVERY still uses the surrogate model (SurrogateObjective). This is the correct setup: PDE targets + surrogate inference = not an inverse crime.

3. **Update TestParameterRecovery.test_recovery_at_noise_levels()**:
   - Change the test to accept the `pde_targets` fixture (or generate PDE targets at the start of the test method)
   - Replace `target_cd_clean = nn_ensemble["target_cd"]` with PDE-generated target_cd
   - The test still needs the nn_ensemble fixture for the surrogate model used during inference
   - Important: The SurrogateObjective targets must use PDE-generated data. The surrogate model is used to simulate I-V curves during optimization, but the TARGET it optimizes toward must be PDE-generated.
   - Keep `nn_ensemble["target_pc"]` for peroxide current if PDE peroxide current is not available (peroxide current is secondary and the surrogate self-consistency there is acceptable). Alternatively, generate PDE peroxide current too if the infrastructure supports it. Document whichever approach is taken.
   - Update the test class docstring to remove "surrogate-generated" and note "PDE-generated targets"

4. **Update TestMultistartBasin.test_multistart_convergence()**:
   - Same pattern: use PDE-generated target_cd instead of `nn_ensemble["target_cd"]`
   - The multistart still uses the surrogate model for optimization -- only the TARGET changes
   - Update docstring

5. **Preserve the nn_ensemble fixture as-is** for TestSurrogateFDConvergence (which legitimately tests surrogate self-consistency -- surrogate targets are correct there)

6. **Update TestGradientConsistencyPDE** to use the extracted module-level `_pde_cd_at_params()` instead of defining it inline (reduces duplication)

7. **Update the module docstring** (lines 1-31) to replace references to "surrogate-generated synthetic data" with "PDE-generated synthetic data" for INV-01 and INV-03

IMPORTANT CONSIDERATIONS:
- The production voltage grid used by the surrogate has `model.n_eta` points. The PDE solve must use the same voltage grid for the targets to be compatible with the surrogate inference (same number of points, same voltage values). Check `model.eta_values` or `model.phi_applied` for the production grid.
- If generating PDE targets at all n_eta voltage points is too expensive for a fixture, consider caching to disk (JSON/npy) similar to `_generate_targets_with_pde()` in `_bv_common.py`. Use a cache file like `StudyResults/inverse_verification/pde_targets_cache.npy`.
- The PDE target generation is itself a `@pytest.mark.slow` operation since it requires Firedrake. TestParameterRecovery is already `@pytest.mark.slow`, so this is consistent.
- TestMultistartBasin is already `@pytest.mark.slow` but does NOT have `@skip_without_firedrake`. If it now depends on PDE targets, it should either: (a) add `@skip_without_firedrake`, or (b) use a cached PDE target that was generated by a previous test run. Option (a) is simpler and clearer.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -c "
import ast, sys

# Parse the test file and verify PDE targets are used
with open('tests/test_inverse_verification.py', 'r') as f:
    source = f.read()

tree = ast.parse(source)

# Check 1: _pde_cd_at_params should exist at module level or as a standalone function
has_pde_helper = '_pde_cd_at_params' in source
assert has_pde_helper, 'Missing _pde_cd_at_params helper function'

# Check 2: TestParameterRecovery should NOT use nn_ensemble target_cd directly for targets
# Look for the pattern: it should reference pde targets, not nn_ensemble['target_cd'] as clean targets
lines = source.split('\n')
in_param_recovery = False
uses_surrogate_targets = False
uses_pde_targets = False
for i, line in enumerate(lines):
    if 'class TestParameterRecovery' in line:
        in_param_recovery = True
    elif in_param_recovery and line.startswith('class '):
        in_param_recovery = False
    if in_param_recovery:
        if 'nn_ensemble[\"target_cd\"]' in line or \"nn_ensemble['target_cd']\" in line:
            if 'target_cd_clean' in line or 'target_cd =' in line:
                uses_surrogate_targets = True
        if '_pde_cd_at_params' in line or 'pde_target' in line.lower():
            uses_pde_targets = True

assert uses_pde_targets, 'TestParameterRecovery does not reference PDE targets'
assert not uses_surrogate_targets, 'TestParameterRecovery still uses surrogate targets from nn_ensemble fixture'

# Check 3: TestMultistartBasin should use PDE targets
in_multistart = False
ms_uses_surrogate = False
ms_uses_pde = False
for i, line in enumerate(lines):
    if 'class TestMultistartBasin' in line:
        in_multistart = True
    elif in_multistart and line.startswith('class '):
        in_multistart = False
    if in_multistart:
        if 'nn_ensemble[\"target_cd\"]' in line or \"nn_ensemble['target_cd']\" in line:
            ms_uses_surrogate = True
        if '_pde_cd_at_params' in line or 'pde_target' in line.lower():
            ms_uses_pde = True

assert ms_uses_pde, 'TestMultistartBasin does not reference PDE targets'
assert not ms_uses_surrogate, 'TestMultistartBasin still uses surrogate targets'

# Check 4: nn_ensemble fixture still exists (needed for surrogate FD test)
assert 'def nn_ensemble' in source, 'nn_ensemble fixture removed (still needed for FD test)'

# Check 5: Module docstring updated
first_docstring_end = source.index('\"\"\"', source.index('\"\"\"') + 3)
module_docstring = source[:first_docstring_end]
assert 'surrogate-generated' not in module_docstring.lower() or 'pde' in module_docstring.lower(), 'Module docstring still says surrogate-generated without PDE context'

print('All inverse-crime verification checks passed')
"
    </automated>
  </verify>
  <done>TestParameterRecovery and TestMultistartBasin use PDE-generated targets (via _pde_cd_at_params or equivalent). The nn_ensemble fixture still provides surrogate targets for TestSurrogateFDConvergence. No inverse crime remains in the parameter recovery or multistart tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed inverse crime in TestParameterRecovery (INV-01) and TestMultistartBasin (INV-03) by switching from surrogate-generated targets to PDE-generated targets. The tests now use PDE forward solves to create ground-truth I-V curves, then attempt to recover parameters using the surrogate model -- eliminating the surrogate-on-surrogate inverse crime.</what-built>
  <how-to-verify>
    1. Run the fast test to confirm no regressions:
       ```
       pytest tests/test_inverse_verification.py -m "not slow" -x -v
       ```
       Expected: TestSurrogateFDConvergence passes

    2. Run the full slow test suite (requires Firedrake + NN ensemble):
       ```
       pytest tests/test_inverse_verification.py -m slow --tb=short -v
       ```
       Expected: All 4 test classes pass (TestSurrogateFDConvergence, TestParameterRecovery, TestGradientConsistencyPDE, TestMultistartBasin)

    3. Verify runtime artifacts were created:
       ```
       ls -la StudyResults/inverse_verification/
       ```
       Expected: parameter_recovery_summary.json, parameter_recovery_details.csv, gradient_pde_consistency.json, multistart_basin.json all exist with recent timestamps

    4. Spot-check parameter_recovery_summary.json confirms PDE targets were used:
       ```
       python -c "import json; d=json.load(open('StudyResults/inverse_verification/parameter_recovery_summary.json')); print(d.get('target_source', d.get('inference_method', 'unknown')))"
       ```
       Expected: Should reference "PDE-generated" targets (not "surrogate-generated")

    NOTE: If TestParameterRecovery soft gates fail at high noise (5%), this may indicate the surrogate cannot recover PDE-generated parameters as well as surrogate-generated ones (expected -- PDE and surrogate differ slightly). The 30% gate at 5% noise is generous. If it fails, report the actual error values so thresholds can be adjusted.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and their error output</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('tests/test_inverse_verification.py').read()); print('syntax ok')"` -- file parses
2. Static check confirms no surrogate-on-surrogate targets in TestParameterRecovery or TestMultistartBasin
3. `pytest tests/test_inverse_verification.py -m "not slow" -x -v` -- fast tests still pass
4. User runs slow tests and confirms all pass with PDE-generated targets
5. JSON artifacts in StudyResults/inverse_verification/ reflect PDE target usage
</verification>

<success_criteria>
- TestParameterRecovery generates targets via PDE forward solves, not model.predict()
- TestMultistartBasin uses PDE-generated targets, not surrogate-generated
- TestSurrogateFDConvergence is unchanged (surrogate self-consistency is legitimate there)
- All slow tests pass when run by user in Firedrake environment
- JSON artifacts confirm PDE target source
</success_criteria>

<output>
After completion, create `.planning/phases/04-inverse-problem-verification/04-03-SUMMARY.md`
</output>
