---
phase: 03-surrogate-fidelity
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_surrogate_fidelity.py
  - tests/test_v13_verification.py
  - StudyResults/surrogate_fidelity/worst_iv_overlay_nn_ensemble.png
  - StudyResults/surrogate_fidelity/worst_iv_overlay_rbf_baseline.png
  - StudyResults/surrogate_fidelity/worst_iv_overlay_pod_rbf_log.png
  - StudyResults/surrogate_fidelity/worst_iv_overlay_pod_rbf_nolog.png
  - StudyResults/surrogate_fidelity/error_vs_params_cd_nn_ensemble.png
  - StudyResults/surrogate_fidelity/error_vs_params_pc_nn_ensemble.png
  - StudyResults/surrogate_fidelity/error_vs_params_cd_rbf_baseline.png
  - StudyResults/surrogate_fidelity/error_vs_params_pc_rbf_baseline.png
  - StudyResults/surrogate_fidelity/error_vs_params_cd_pod_rbf_log.png
  - StudyResults/surrogate_fidelity/error_vs_params_pc_pod_rbf_log.png
  - StudyResults/surrogate_fidelity/error_vs_params_cd_pod_rbf_nolog.png
  - StudyResults/surrogate_fidelity/error_vs_params_pc_pod_rbf_nolog.png
autonomous: true
requirements:
  - SUR-01
  - SUR-03

must_haves:
  truths:
    - "Worst-case I-V overlay plots show top 3 highest-NRMSE samples per model with surrogate vs PDE curves overlaid"
    - "Error-vs-parameter scatter plots show NRMSE vs each of 4 parameters, separate for CD and PC"
    - "The subsumed TestSurrogateVsPDEConsistency class is removed from test_v13_verification.py without breaking other tests"
  artifacts:
    - path: "StudyResults/surrogate_fidelity/worst_iv_overlay_nn_ensemble.png"
      provides: "Worst-case I-V overlay for NN ensemble"
    - path: "StudyResults/surrogate_fidelity/error_vs_params_cd_nn_ensemble.png"
      provides: "CD error vs parameter scatter for NN ensemble"
    - path: "StudyResults/surrogate_fidelity/error_vs_params_pc_nn_ensemble.png"
      provides: "PC error vs parameter scatter for NN ensemble"
  key_links:
    - from: "tests/test_surrogate_fidelity.py"
      to: "StudyResults/surrogate_fidelity/*.png"
      via: "matplotlib savefig in test body"
      pattern: "savefig"
    - from: "tests/test_v13_verification.py"
      to: "tests/test_surrogate_fidelity.py"
      via: "Test 5 removed from v13, subsumed by fidelity test"
      pattern: "TestSurrogateVsPDEConsistency"
---

<objective>
Add diagnostic plots to the surrogate fidelity test (worst-case I-V overlays, error-vs-parameter scatter) and remove the subsumed TestSurrogateVsPDEConsistency from test_v13_verification.py.

Purpose: Visual diagnostics enable quick identification of surrogate failure modes (parameter regions, voltage ranges). Removing Test 5 eliminates redundancy with the comprehensive fidelity test.
Output: 12 PNG diagnostic plots in `StudyResults/surrogate_fidelity/`; cleaned `test_v13_verification.py`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-surrogate-fidelity/03-CONTEXT.md
@.planning/phases/03-surrogate-fidelity/03-RESEARCH.md
@.planning/phases/03-surrogate-fidelity/03-01-SUMMARY.md
@tests/test_surrogate_fidelity.py
@tests/test_v13_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostic plots to surrogate fidelity test</name>
  <files>tests/test_surrogate_fidelity.py</files>
  <action>
Add plot-generation logic to `tests/test_surrogate_fidelity.py`. Use the existing module-scoped fixtures (`all_models`, `all_metrics`, `holdout_data`) from Plan 01.

1. **Add a module-scoped fixture `generate_plots`** (depends on `all_models`, `all_metrics`, `holdout_data`):

   For each model in MODEL_NAMES:

   a) **Worst-case I-V overlay** (top 3 worst CD NRMSE samples):
      - Get `cd_nrmse_per_sample` from `all_metrics[model_name]`
      - Find top 3 indices: `np.argsort(cd_nrmse)[-3:][::-1]`
      - Create 1x3 subplot figure (figsize 15x4)
      - For each of 3 worst samples: plot PDE truth (black solid) and surrogate prediction (red dashed) vs `phi_applied`. Title with sample index and NRMSE percentage.
      - Save as `worst_iv_overlay_{model_name}.png` at 150 DPI
      - Close figure

   b) **Error vs parameter scatter -- CD**:
      - Create 1x4 subplot figure (figsize 16x4)
      - For each of 4 parameters (k0_1, k0_2, alpha_1, alpha_2): scatter plot of NRMSE*100 vs parameter value
      - Use log scale on x-axis for k0_1 and k0_2 (they span orders of magnitude)
      - Scatter point size=8, alpha=0.5
      - Suptitle: `"{model_name} -- CD Error vs Parameters"`
      - Save as `error_vs_params_cd_{model_name}.png` at 150 DPI
      - Close figure

   c) **Error vs parameter scatter -- PC** (same layout as CD but with pc_nrmse):
      - Save as `error_vs_params_pc_{model_name}.png` at 150 DPI
      - Close figure

   Return list of generated file paths for test verification.

2. **Add test methods** to `TestSurrogateFidelity`:
   - `test_worst_iv_overlay_plots_generated(self, generate_plots)`: Assert all 4 worst-case overlay PNGs exist and have size > 0.
   - `test_error_scatter_plots_generated(self, generate_plots)`: Assert all 8 error-vs-parameter PNGs exist (4 models x 2 outputs).

For the worst-case overlay, call `model.predict_batch(test_params[idx:idx+1])` to get the surrogate prediction for each sample. The PDE truth is in `holdout_data` test arrays.

Use `matplotlib.use("Agg")` (already set in Plan 01). Always `plt.close(fig)` after saving. Never call `plt.show()`.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_surrogate_fidelity.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 4 worst-case I-V overlay PNGs exist in StudyResults/surrogate_fidelity/
    - 8 error-vs-parameter scatter PNGs exist (4 models x CD/PC)
    - All tests pass including plot verification tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove subsumed TestSurrogateVsPDEConsistency from test_v13_verification.py</name>
  <files>tests/test_v13_verification.py</files>
  <action>
Remove the `TestSurrogateVsPDEConsistency` class (Test 5) from `tests/test_v13_verification.py`:

1. Remove the entire class `TestSurrogateVsPDEConsistency` starting at approximately line 604 (including the `# Test 5:` comment block above it) through the end of the class.

2. Do NOT remove the `nn_ensemble` module-scoped fixture -- it is still used by Tests 1, 3, 6, 7 in the same file.

3. Verify no other test in the file references `TestSurrogateVsPDEConsistency` or depends on it.

4. Run the remaining tests in `test_v13_verification.py` to confirm nothing is broken. The file should still have Tests 1-4, 6-7 (or whatever the actual numbering is minus Test 5).

5. Add a brief comment where Test 5 was: `# Test 5 (Surrogate vs PDE consistency) removed -- subsumed by tests/test_surrogate_fidelity.py`
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_v13_verification.py --collect-only 2>&1 | tail -20 && python -c "import ast; ast.parse(open('tests/test_v13_verification.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - TestSurrogateVsPDEConsistency class is removed from test_v13_verification.py
    - Remaining tests in test_v13_verification.py still collect and parse without errors
    - Comment documents the removal and points to the new test file
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_surrogate_fidelity.py -x -v` passes all tests including plot verification
- `pytest tests/test_v13_verification.py --collect-only` shows no Test 5 and no import errors
- `ls StudyResults/surrogate_fidelity/*.png` shows 12 PNG files
- No Firedrake imports in test_surrogate_fidelity.py
</verification>

<success_criteria>
Diagnostic plots generated for all 4 models (12 PNGs total). Subsumed Test 5 removed cleanly. All tests pass in both test files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-surrogate-fidelity/03-02-SUMMARY.md`
</output>
