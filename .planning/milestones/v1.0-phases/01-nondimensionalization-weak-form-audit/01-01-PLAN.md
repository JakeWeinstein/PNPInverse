---
phase: 01-nondimensionalization-weak-form-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_nondim_audit.py
  - tests/test_nondim.py
autonomous: true
requirements:
  - FWD-04

must_haves:
  truths:
    - "Existing test_nondim.py assertions are validated against textbook formulas for thermal voltage, Debye length, and scale relationships"
    - "Roundtrip tests prove physical -> nondim -> physical identity for D, c0, c_inf, phi, dt/t_end, kappa across 1/2/4-species configs"
    - "Derived quantities (Debye length, flux scale, current density scale) are consistent with their component scales"
    - "BV-specific scaling (k0, c_ref, E_eq) roundtrips correctly for 4-species config"
  artifacts:
    - path: "tests/test_nondim_audit.py"
      provides: "Textbook formula verification tests"
      min_lines: 80
    - path: "tests/test_nondim.py"
      provides: "Extended with roundtrip test classes"
      contains: "TestNondimRoundtrip"
  key_links:
    - from: "tests/test_nondim_audit.py"
      to: "Nondim/scales.py"
      via: "import build_physical_scales"
      pattern: "from Nondim\\.scales import build_physical_scales"
    - from: "tests/test_nondim.py"
      to: "Nondim/transform.py"
      via: "import build_model_scaling"
      pattern: "from Nondim\\.transform import build_model_scaling"
---

<objective>
Validate existing nondimensionalization tests against textbook formulas, then add comprehensive roundtrip tests proving physical -> nondim -> physical identity for all parameter types across 1/2/4-species configurations.

Purpose: FWD-04 requires proof that the nondim transform is mathematically correct. The user has no confidence in existing tests, so validation is a hard prerequisite before adding roundtrip coverage.
Output: Validated existing tests, new textbook audit tests, new roundtrip tests for all parameter types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-nondimensionalization-weak-form-audit/01-CONTEXT.md
@.planning/phases/01-nondimensionalization-weak-form-audit/01-RESEARCH.md

@Nondim/transform.py
@Nondim/scales.py
@Nondim/constants.py
@Forward/bv_solver/nondim.py
@tests/test_nondim.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Nondim/constants.py:
  FARADAY_CONSTANT, GAS_CONSTANT, DEFAULT_TEMPERATURE_K,
  VACUUM_PERMITTIVITY_F_PER_M, DEFAULT_RELATIVE_PERMITTIVITY_WATER,
  MOLAR_TO_MOL_PER_M3

From Nondim/scales.py:
  class NondimScales (dataclass):
    d_ref_m2_s, c_ref_mol_m3, c_inf_mol_m3, l_ref_m,
    thermal_voltage_v, time_scale_s, kappa_scale_m_s,
    molar_flux_scale, current_density_scale_a_m2,
    debye_length_m, debye_ratio, diffusivity_scale_m2_s,
    concentration_scale_mol_m3, potential_scale_v
    def to_dict() -> dict

  def build_physical_scales(d_species_m2_s, c_bulk_m, ...) -> NondimScales

From Nondim/transform.py:
  def build_model_scaling(params, n_species, dt, t_end, D_vals, c0_vals, phi_applied, phi0, ...) -> dict
  # Returns dict with keys: D_model_vals, c0_model_vals, c_inf_model_vals,
  # phi_applied_model, dt_model, t_end_model, kappa_model_vals,
  # diffusivity_scale_m2_s, concentration_scale_mol_m3, potential_scale_v,
  # time_scale_s, kappa_scale_m_s, ...

From Forward/bv_solver/nondim.py:
  def _add_bv_reactions_scaling_to_transform(transform_dict, bv_reactions, nondim_cfg) -> None
  # Mutates transform_dict to add BV-specific scaling for k0, c_ref, E_eq

From tests/conftest.py:
  @pytest.fixture default_nondim_kwargs -> dict  # 2-species test params
  skip_without_firedrake = pytest.mark.skipif(...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Textbook audit of existing tests and new audit test file</name>
  <files>tests/test_nondim_audit.py</files>
  <action>
Create `tests/test_nondim_audit.py` containing textbook verification tests that independently validate the correctness of `Nondim/scales.py` and `Nondim/constants.py` computations. This file serves as the formal audit record.

**Step 1: Read and hand-verify existing tests.**
Read `tests/test_nondim.py` thoroughly. For each class (TestConstants, TestNondimScales, etc.), verify that the expected values in assertions match textbook formulas. Document which tests were audited at the top of the new file in a docstring.

**Step 2: Write independent textbook verification tests.**
Create class `TestTextbookVerification` with tests that compute expected values from first principles (NOT by calling the code under test for both sides):

- `test_thermal_voltage_textbook`: V_T = R*T/F. Use NIST CODATA values: R=8.314462618, F=96485.3329, T=298.15. Assert `build_physical_scales(...).thermal_voltage_v == pytest.approx(V_T, rel=1e-10)`. Also assert sanity: ~25.69 mV.

- `test_debye_length_textbook`: lambda_D = sqrt(eps_r * eps_0 * V_T / (F * c_ref)). Use 0.1M (100 mol/m3) 1:1 electrolyte. Assert result is ~0.97 nm range (0.5e-9 to 2e-9).

- `test_diffusivity_scale_is_geometric_mean`: For D = [1e-9, 4e-9], verify d_ref = exp(mean(log(D))) = 2e-9. Compare against `build_physical_scales()` output.

- `test_concentration_scale_is_max_abs`: For c_bulk = [0.1, 0.5] M, verify c_ref = max(abs(c_all)) * 1000 mol/m3. Compare against output.

- `test_time_scale_formula`: t_ref = L^2 / D_ref. Verify against `time_scale_s`.

- `test_kappa_scale_formula`: kappa_ref = D_ref / L_ref. Verify against `kappa_scale_m_s`.

- `test_molar_flux_scale_formula`: J_ref = D_ref * c_ref / L_ref. Verify against `molar_flux_scale`.

- `test_current_density_scale_formula`: j_ref = F * J_ref. Verify against `current_density_scale_a_m2`.

- `test_debye_ratio_formula`: eps_hat = (lambda_D / L_ref)^2. Verify against `debye_ratio`.

**Step 3: Add audit summary docstring** at the top of the file listing which existing tests in `test_nondim.py` were reviewed and confirmed correct, by class name and test name.

Tolerance for all textbook comparisons: `rel=1e-10` (tighter than roundtrip tests because these are exact formulas, not chained operations).
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_nondim_audit.py -x -v</automated>
  </verify>
  <done>All textbook verification tests pass. Audit docstring documents which existing tests were reviewed and confirmed correct. Each textbook test computes expected values from first principles and compares against library output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nondim roundtrip tests for all parameter types and species configs</name>
  <files>tests/test_nondim.py</files>
  <behavior>
    - Roundtrip D: D_model * D_scale == D_phys (rel=1e-12) for 1/2/4 species
    - Roundtrip c0: c0_model * c_scale == c0_phys (rel=1e-12) for 1/2/4 species
    - Roundtrip c_inf: c_inf_model * c_scale == c_inf_phys (rel=1e-12) for 1/2/4 species
    - Roundtrip phi: phi_model * phi_scale == phi_phys (rel=1e-12)
    - Roundtrip dt/t_end: dt_model * t_scale == dt_phys, t_end_model * t_scale == t_end_phys (rel=1e-12)
    - Roundtrip kappa: kappa_model * kappa_scale == kappa_phys (rel=1e-12)
    - Derived quantity consistency: Debye length, flux scale, current density scale are consistent with their component scales
    - BV-specific scaling: k0, c_ref, E_eq roundtrip correctly for 4-species config via _add_bv_reactions_scaling_to_transform
    - Parametrized synthetic values: cover different orders of magnitude (e.g., D from 1e-11 to 1e-7)
    - v13 production parameter values included as a parametrize case
  </behavior>
  <action>
Add new test classes to `tests/test_nondim.py` (do NOT modify existing classes):

**Class `TestNondimRoundtrip`:**
Use `@pytest.mark.parametrize` with cases:
- 1-species: D=[9.311e-9], c0=[100.0], c_inf=[10.0], phi=0.05, kappa=[1e-4]
- 2-species: D=[9.311e-9, 5.273e-9], c0=[100.0, 50.0], c_inf=[10.0, 5.0], phi=0.1, kappa=[1e-4, 2e-4]
- 4-species (v13): D=[1.97e-9, 1.4e-9, 9.311e-9, 1.792e-9], c0=[0.27, 0.01, 0.01, 0.01] mol/m3, c_inf=[0.27, 0.01, 0.01, 0.01], phi=0.05, kappa=[1e-4]*4
  (Note: search codebase for exact v13 params if available; use these as fallback)
- Synthetic edge cases: very small D=[1e-11, 1e-11], very large c0=[1e6, 1e6], etc.

Each parametrized test calls `build_model_scaling()` then asserts:
- `D_model_vals[i] * diffusivity_scale_m2_s == pytest.approx(D_phys[i], rel=1e-12)` for all i
- Same pattern for c0, c_inf, phi, dt, t_end, kappa

Use dt_phys=0.001, t_end_phys=1.0 for all cases.

**Class `TestDerivedQuantityConsistency`:**
Tests that derived quantities are consistent with their component scales (not just roundtrip, but cross-checking relationships):
- `flux_scale == D_ref * c_ref / L_ref`
- `current_density_scale == F * flux_scale`
- `debye_ratio == (debye_length / L_ref)^2`

Use the same parametrized species configs.

**Class `TestBVScalingRoundtrip`:**
Test that BV-specific scaling (k0, c_ref, E_eq) roundtrips correctly for the 4-species multi-reaction config. This requires calling `_add_bv_reactions_scaling_to_transform()` after `build_model_scaling()`. Configure a mock BV reactions list with known k0, c_ref, E_eq values and verify they roundtrip through the BV scaling.

Read `Forward/bv_solver/nondim.py` to understand the exact scaling applied to k0/c_ref/E_eq, then write assertions that invert it.

**Important:** Only test `enabled=True` mode per user decision. Use `pytest.approx(val, rel=1e-12)` for all roundtrip assertions.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_nondim.py -x -v -k "Roundtrip or DerivedQuantity or BVScaling"</automated>
  </verify>
  <done>Roundtrip tests cover D, c0, c_inf, phi, dt/t_end, kappa for 1/2/4-species configs (including v13 production values and synthetic edge cases). Derived quantity consistency tests verify cross-scale relationships. BV-specific scaling roundtrips correctly. All pass with rel=1e-12 tolerance. Existing tests unmodified and still pass.</done>
</task>

</tasks>

<verification>
Run full nondim test suite to confirm no regressions and all new tests pass:
```bash
cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_nondim.py tests/test_nondim_audit.py -v
```
</verification>

<success_criteria>
- All existing tests in test_nondim.py still pass (no regressions)
- Textbook audit tests pass in test_nondim_audit.py with independent formula verification
- Roundtrip tests pass for all parameter types across 1/2/4-species configs at rel=1e-12
- BV-specific scaling (k0, c_ref, E_eq) roundtrips correctly for 4-species config
- Audit docstring documents which existing tests were reviewed and confirmed correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-nondimensionalization-weak-form-audit/01-01-SUMMARY.md`
</output>
