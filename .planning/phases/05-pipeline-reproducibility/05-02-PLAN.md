---
phase: 05-pipeline-reproducibility
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/test_pipeline_reproducibility.py
  - StudyResults/pipeline_reproducibility/regression_baselines.json
autonomous: false
requirements: [PIP-01, PIP-02]

must_haves:
  truths:
    - "Full 7-phase pipeline (S1-S5 + P1-P2) produces deterministic results within solver tolerance rel=1e-4"
    - "Running pytest --update-baselines updates the full_pipeline section of regression_baselines.json"
    - "Full pipeline test is marked @pytest.mark.slow and @skip_without_firedrake"
    - "Test compares final inferred parameters (k0_1, k0_2, alpha_1, alpha_2), final loss, S1 output, and P2 output"
    - "Full pipeline test runs via subprocess (avoids Firedrake/PyTorch segfault)"
  artifacts:
    - path: "tests/test_pipeline_reproducibility.py"
      provides: "TestFullPipelineReproducibility class with subprocess pipeline execution"
      contains: "TestFullPipelineReproducibility"
    - path: "StudyResults/pipeline_reproducibility/regression_baselines.json"
      provides: "Full pipeline baseline values alongside surrogate-only baselines"
      contains: "full_pipeline"
  key_links:
    - from: "tests/test_pipeline_reproducibility.py"
      to: "scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py"
      via: "subprocess invocation of full v13 pipeline"
      pattern: "Infer_BVMaster_charged_v13_ultimate"
    - from: "tests/test_pipeline_reproducibility.py"
      to: "StudyResults/master_inference_v13/master_comparison_v13.csv"
      via: "CSV parsing of pipeline output"
      pattern: "master_comparison_v13\\.csv"
---

<objective>
Add TestFullPipelineReproducibility: a slow integration test that runs the full v13 7-phase pipeline (S1-S5 + P1-P2) via subprocess, parses the output CSV, and compares final inferred parameters + loss + key intermediates (S1, P2) against saved baselines at rel=1e-4 (solver tolerance).

Purpose: Proves the complete inference pipeline is deterministic end-to-end (PIP-01) and catches numerical regression in PDE-refined outputs (PIP-02).
Output: Working slow test + full_pipeline baselines in JSON.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-reproducibility/05-CONTEXT.md
@.planning/phases/05-pipeline-reproducibility/05-RESEARCH.md
@.planning/phases/05-pipeline-reproducibility/05-01-SUMMARY.md
@tests/test_pipeline_reproducibility.py
@tests/conftest.py
@scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py

<interfaces>
<!-- From 05-01: baseline helpers already in test_pipeline_reproducibility.py -->
```python
_load_baselines(path: str) -> dict | None
_save_baselines(path: str, data: dict) -> None
_format_diff_table(entries: list[tuple[str, float, float, float]]) -> tuple[str, bool]
_assert_baselines(current: dict, section: str, tolerance: float, baselines: dict, update_baselines: bool, baselines_path: str) -> None
_BASELINES_PATH: str  # Path to regression_baselines.json
_ROOT: str  # Project root
```

<!-- From conftest.py (added in 05-01) -->
```python
@pytest.fixture()
def update_baselines(request) -> bool
```

<!-- From conftest.py (existing) -->
```python
skip_without_firedrake  # Skip decorator
FIREDRAKE_AVAILABLE  # Bool flag
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TestFullPipelineReproducibility with subprocess execution</name>
  <files>tests/test_pipeline_reproducibility.py, StudyResults/pipeline_reproducibility/regression_baselines.json</files>
  <action>
In tests/test_pipeline_reproducibility.py, add TestFullPipelineReproducibility:

1. Mark the class with `@pytest.mark.slow` and `@skip_without_firedrake`.

2. Module-scoped fixture `full_pipeline_results()`:
   - Run the v13 pipeline via subprocess: `[sys.executable, "scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py", "--noise-percent", "0", "--noise-seed", "42"]` with cwd=_ROOT, timeout=900, capture_output=True, text=True.
   - Check returncode; if non-zero, pytest.fail with stderr.
   - Parse `StudyResults/master_inference_v13/master_comparison_v13.csv` using csv.DictReader.
   - Extract rows for phases S1, S2, S3, S4, S5, P1, P2 (the CSV has a "Phase" or "phase" column -- inspect the actual v13 script to find exact column names and row labels).
   - Return a dict with: s1_k0, s1_alpha, p2_k0, p2_alpha, p2_loss, final_k0, final_alpha, final_loss (final = last phase in CSV, typically P2).
   - Key: inspect the CSV format written by the v13 script to determine exact column names (likely "k0_1", "k0_2", "alpha_1", "alpha_2", "loss" or similar).

3. Test methods:
   - `test_full_pipeline_parameters_reproducible(self, full_pipeline_results, update_baselines)`: Build current dict with s1 and p2 and final parameter values. Call _assert_baselines with section="full_pipeline", tolerance=1e-4.
   - `test_full_pipeline_loss_reproducible(self, full_pipeline_results, update_baselines)`: Build current dict with p2_loss and final_loss. Call _assert_baselines with section="full_pipeline_loss", tolerance=1e-4, abs_floor=1e-8.
   - `test_intermediate_s1_reproducible(self, full_pipeline_results, update_baselines)`: S1 intermediate values separately at rel=1e-4 (S1 uses surrogate only but goes through subprocess so CSV precision applies).

4. After implementing, generate baselines by running:
   `pytest tests/test_pipeline_reproducibility.py::TestFullPipelineReproducibility --update-baselines -x -v -m slow`
   This will take several minutes (PDE solves). If the environment does not have Firedrake, note that baselines must be generated by the user.

5. Verify by running without --update-baselines:
   `pytest tests/test_pipeline_reproducibility.py::TestFullPipelineReproducibility -x -v -m slow`

Key details:
- Use subprocess, NOT direct import, for the full pipeline (avoids Firedrake/PyTorch segfault).
- PDE outputs have solver tolerance ~1e-4, so rel=1e-4 is correct for P1/P2 phases.
- Use pytest.approx with both rel and abs for objective function values (per locked decision: rel=1e-4, abs=1e-8).
- Parse the CSV the v13 script already writes -- do NOT modify the production script.
- If Firedrake is not available, the test will be skipped via @skip_without_firedrake.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -c "from tests.test_pipeline_reproducibility import TestFullPipelineReproducibility; print('class imports ok')"</automated>
  </verify>
  <done>TestFullPipelineReproducibility class exists with subprocess pipeline execution, CSV parsing, and baseline comparison at rel=1e-4</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies pipeline reproducibility tests</name>
  <what-built>
Full pipeline reproducibility test (TestFullPipelineReproducibility) that runs the v13 7-phase pipeline via subprocess, parses output CSV, and compares against saved baselines. Plus the surrogate-only test from Plan 05-01.
  </what-built>
  <how-to-verify>
1. Run the fast surrogate-only tests (no Firedrake needed):
   ```
   pytest tests/test_pipeline_reproducibility.py -x -v -m "not slow"
   ```
   Expected: TestSurrogateReproducibility tests pass.

2. If Firedrake is available, generate full pipeline baselines:
   ```
   pytest tests/test_pipeline_reproducibility.py --update-baselines -x -v
   ```
   Then verify against those baselines:
   ```
   pytest tests/test_pipeline_reproducibility.py -x -v
   ```
   Expected: All tests pass (surrogate-only at rel=1e-10, full pipeline at rel=1e-4).

3. Inspect `StudyResults/pipeline_reproducibility/regression_baselines.json`:
   - Has "metadata" section with git_commit, timestamps, versions
   - Has "surrogate_only" section with parameter values
   - Has "full_pipeline" section with parameter values (if step 2 was run)

4. Verify missing-baselines behavior:
   ```
   mv StudyResults/pipeline_reproducibility/regression_baselines.json /tmp/baselines_backup.json
   pytest tests/test_pipeline_reproducibility.py::TestSurrogateReproducibility -x 2>&1 | head -20
   mv /tmp/baselines_backup.json StudyResults/pipeline_reproducibility/regression_baselines.json
   ```
   Expected: Clear "Run with --update-baselines" error message.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pytest tests/test_pipeline_reproducibility.py -x -v -m "not slow"` -- surrogate tests pass
2. `pytest tests/test_pipeline_reproducibility.py -x -v` -- all tests pass (if Firedrake available)
3. regression_baselines.json contains both surrogate_only and full_pipeline sections
4. Removing baselines file causes clear error message
5. --update-baselines regenerates baselines without error
</verification>

<success_criteria>
- Full pipeline test runs via subprocess (no Firedrake import in test process)
- Final parameters match baselines at rel=1e-4
- S1 intermediate and P2 intermediate values captured and compared
- Objective function compared with rel=1e-4, abs=1e-8
- Test is properly marked @pytest.mark.slow + @skip_without_firedrake
- User confirms both surrogate-only and full-pipeline tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-reproducibility/05-02-SUMMARY.md`
</output>
