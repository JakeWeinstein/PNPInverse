---
phase: 01-nondimensionalization-weak-form-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_nondim.py
autonomous: true
gap_closure: true
requirements:
  - FWD-04

must_haves:
  truths:
    - "All 116 nondim tests pass (or skip gracefully) without manual intervention in any environment"
    - "TestBVScalingRoundtrip tests skip cleanly when Firedrake is unavailable"
  artifacts:
    - path: "tests/test_nondim.py"
      provides: "BV scaling roundtrip tests with skip guard"
      contains: "skip_without_firedrake"
  key_links:
    - from: "tests/test_nondim.py"
      to: "tests/conftest.py"
      via: "skip_without_firedrake marker import"
      pattern: "skip_without_firedrake"
---

<objective>
Fix the 5 failing TestBVScalingRoundtrip tests by adding the @skip_without_firedrake decorator so they skip gracefully when Firedrake is unavailable, matching the pattern used by MMS smoke tests.

Purpose: Close the one verification gap blocking Phase 1 Success Criterion 3 ("All nondim and audit tests pass without manual intervention").
Output: Updated tests/test_nondim.py with skip guard on TestBVScalingRoundtrip.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-nondimensionalization-weak-form-audit/01-VERIFICATION.md

<interfaces>
<!-- From tests/conftest.py (already available to all test modules): -->
```python
skip_without_firedrake = pytest.mark.skipif(
    not FIREDRAKE_AVAILABLE,
    reason="Firedrake is not installed or not importable in this environment",
)
```

<!-- TestBVScalingRoundtrip in tests/test_nondim.py (line 765) uses lazy imports: -->
```python
from Forward.bv_solver.nondim import _add_bv_reactions_scaling_to_transform
```
<!-- This import triggers Forward/__init__.py -> firedrake import chain -->
<!-- The decorator must be applied at the class level so all 5 test methods skip -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skip_without_firedrake guard to TestBVScalingRoundtrip</name>
  <files>tests/test_nondim.py</files>
  <action>
  1. Import `skip_without_firedrake` from `conftest` at the top of the file (it is already available via pytest's conftest mechanism, but an explicit import or usage of the marker is needed). Check how other test files in the project use it -- `test_mms_smoke.py` imports it from `conftest`.

  2. Add the `@skip_without_firedrake` decorator to the `TestBVScalingRoundtrip` class (line 765). Apply it at the class level so all 5 test methods (`test_k0_roundtrip`, `test_c_ref_roundtrip`, `test_E_eq_roundtrip`, `test_current_density_scale_roundtrip`, `test_all_bv_params_consistent`) skip together.

  3. The decorator pattern should match what `test_mms_smoke.py` uses:
     ```python
     from conftest import skip_without_firedrake

     @skip_without_firedrake
     class TestBVScalingRoundtrip:
         ...
     ```

     If `skip_without_firedrake` is already importable without explicit import (via conftest auto-discovery), just use the decorator directly. Check whether existing test files import it explicitly or rely on conftest injection.

  Do NOT modify any test logic, assertions, or imports within the test methods. Only add the skip guard.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_nondim.py tests/test_nondim_audit.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All nondim tests either pass or skip (no failures, no errors). TestBVScalingRoundtrip tests show "SKIPPED" when Firedrake is unavailable. Zero test errors in the full nondim test suite.</done>
</task>

</tasks>

<verification>
Run the full nondim + audit test suite and confirm zero failures:
```bash
python -m pytest tests/test_nondim.py tests/test_nondim_audit.py -v --tb=short
```
Expected: 111 passed, 5 skipped (or all 116 passed if Firedrake is available). Zero errors.
</verification>

<success_criteria>
- `pytest tests/test_nondim.py tests/test_nondim_audit.py` exits with code 0
- TestBVScalingRoundtrip tests skip gracefully with "Firedrake not available" reason when Firedrake is absent
- No other tests are affected by the change
</success_criteria>

<output>
After completion, create `.planning/phases/01-nondimensionalization-weak-form-audit/01-03-SUMMARY.md`
</output>
