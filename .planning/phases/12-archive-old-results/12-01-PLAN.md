---
phase: 12-archive-old-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StudyResults/v14_pde_only/
  - StudyResults/tmp/
  - StudyResults/tmp_replay_diag_smoke/
  - StudyResults/tmp_replay_diag_smoke2/
  - StudyResults/convergence_study_summary.txt
  - StudyResults/master_inference_v5_log.txt
  - archive/StudyResults/
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "~96 old StudyResults directories exist under archive/StudyResults/ with contents intact"
    - "StudyResults/v14_pde_only/ no longer exists in the repo"
    - "Temporary artifacts (tmp dirs, loose log files) no longer exist"
    - "Current results (v13, v14, V&V dirs) remain in their original StudyResults/ locations"
    - "Loose .md summary files are archived, not deleted"
  artifacts:
    - path: "archive/StudyResults/"
      provides: "Archive location for old results"
    - path: "StudyResults/master_inference_v13/"
      provides: "Kept v13 results (unchanged)"
    - path: "StudyResults/v14/"
      provides: "Kept v14 results (unchanged)"
    - path: "StudyResults/inverse_verification/"
      provides: "Kept V&V results (unchanged)"
    - path: "StudyResults/surrogate_fidelity/"
      provides: "Kept V&V results (unchanged)"
    - path: "StudyResults/mms_convergence/"
      provides: "Kept V&V results (unchanged)"
    - path: "StudyResults/pipeline_reproducibility/"
      provides: "Kept V&V results (unchanged)"
    - path: "StudyResults/target_cache/"
      provides: "Kept cache (unchanged)"
  key_links:
    - from: "writeups/vv_report/generate_figures.py"
      to: "StudyResults/inverse_verification, surrogate_fidelity, mms_convergence, pipeline_reproducibility"
      via: "file path references"
      pattern: "StudyResults/(inverse_verification|surrogate_fidelity|mms_convergence|pipeline_reproducibility)"
---

<objective>
Archive old StudyResults directories and delete bad/temporary outputs.

Purpose: Clean the working directory so only current (v13, v14, V&V) results remain visible in StudyResults/, while preserving historical results in archive/StudyResults/.
Output: Clean StudyResults/ with ~7 kept directories; archive/StudyResults/ with ~96 archived directories + 4 loose .md files; bad outputs and tmp artifacts deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-archive-old-results/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete bad outputs and temporary artifacts</name>
  <files>
    StudyResults/v14_pde_only/
    StudyResults/tmp/
    StudyResults/tmp_replay_diag_smoke/
    StudyResults/tmp_replay_diag_smoke2/
    StudyResults/convergence_study_summary.txt
    StudyResults/master_inference_v5_log.txt
  </files>
  <action>
Delete the following items from StudyResults/ using `git rm -r`:

1. `StudyResults/v14_pde_only/` -- bad output per ARCH-02
2. `StudyResults/tmp/` -- temporary debugging artifact
3. `StudyResults/tmp_replay_diag_smoke/` -- temporary debugging artifact
4. `StudyResults/tmp_replay_diag_smoke2/` -- temporary debugging artifact
5. `StudyResults/convergence_study_summary.txt` -- loose log file
6. `StudyResults/master_inference_v5_log.txt` -- loose log file

Commit with message: `chore(12): delete bad outputs and temporary artifacts`

Per user decision: this is the first of two commits (delete first, then archive).
  </action>
  <verify>
    <automated>test ! -d StudyResults/v14_pde_only && test ! -d StudyResults/tmp && test ! -d StudyResults/tmp_replay_diag_smoke && test ! -d StudyResults/tmp_replay_diag_smoke2 && test ! -f StudyResults/convergence_study_summary.txt && test ! -f StudyResults/master_inference_v5_log.txt && echo "PASS: all deletions confirmed"</automated>
  </verify>
  <done>v14_pde_only/, all tmp dirs, and loose log files no longer exist in StudyResults/. Changes committed.</done>
</task>

<task type="auto">
  <name>Task 2: Move old directories and loose .md files to archive</name>
  <files>
    archive/StudyResults/
    StudyResults/
  </files>
  <action>
Create `archive/StudyResults/` directory. Move all StudyResults directories and loose .md files to `archive/StudyResults/` EXCEPT the 7 keep-set items:

**Keep in place (do NOT move):**
- master_inference_v13
- v14
- inverse_verification
- surrogate_fidelity
- mms_convergence
- pipeline_reproducibility
- target_cache

**Move to archive/StudyResults/ (use `git mv`):**
Every other directory and the 4 loose .md summary files:
- phase3_inference_summary.md
- phase4_steric_inference_summary.md
- phase5_extended_inference_summary.md
- phase6_symmetric_inference_summary.md

Implementation approach:
1. `mkdir -p archive/StudyResults`
2. For each item in StudyResults/ that is NOT in the keep set (and was not deleted in Task 1), run `git mv StudyResults/{item} archive/StudyResults/{item}`
3. Verify the keep-set directories still exist in StudyResults/
4. Commit with message: `chore(12): archive old StudyResults to archive/StudyResults/`

Flat dump -- no subdirectory grouping. Original directory names preserved. No manifest or README needed (per user decision).
  </action>
  <verify>
    <automated>test -d archive/StudyResults/master_inference_v2 && test -d archive/StudyResults/bv_iv_curve && test -f archive/StudyResults/phase3_inference_summary.md && test -d StudyResults/master_inference_v13 && test -d StudyResults/v14 && test -d StudyResults/inverse_verification && test -d StudyResults/surrogate_fidelity && test -d StudyResults/mms_convergence && test -d StudyResults/pipeline_reproducibility && test -d StudyResults/target_cache && echo "PASS: archive and keep sets verified"</automated>
  </verify>
  <done>~96 old directories and 4 loose .md files exist under archive/StudyResults/ with contents intact. The 7 keep-set directories remain in StudyResults/. Changes committed.</done>
</task>

</tasks>

<verification>
1. `ls StudyResults/ | wc -l` should return 7 (the keep-set directories only)
2. `ls archive/StudyResults/ | wc -l` should return ~100 (96 dirs + 4 .md files)
3. `test ! -d StudyResults/v14_pde_only` confirms ARCH-02
4. `test -d archive/StudyResults/master_inference_v2` confirms ARCH-01
5. `git log --oneline -2` shows the two commits (delete, then archive)
</verification>

<success_criteria>
- StudyResults/ contains exactly 7 directories (keep set) and no loose files
- archive/StudyResults/ contains ~100 items (96 dirs + 4 .md files) with contents intact
- v14_pde_only/ is gone (ARCH-02)
- Old results are archived, not deleted (ARCH-01)
- Two clean commits in history per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/12-archive-old-results/12-01-SUMMARY.md`
</output>
