---
phase: 07-baseline-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/studies/run_multi_seed_v13.py
  - tests/test_diagnostic_metadata.py
  - tests/test_multi_seed_aggregation.py
autonomous: true
requirements: [DIAG-01, AUDT-04]
must_haves:
  truths:
    - "Multi-seed wrapper runs v13 pipeline across 20 noise seeds via subprocess, producing per-seed results"
    - "Summary CSV reports per-parameter median relative error, IQR (25th/75th), and worst-case (max) for k0_1, k0_2, alpha_1, alpha_2"
    - "Box plots and per-seed scatter plots are auto-generated as PNG files"
    - "JSON metadata sidecar documents the diagnostic tool's justification per AUDT-04"
  artifacts:
    - path: "scripts/studies/run_multi_seed_v13.py"
      provides: "Multi-seed v13 wrapper with subprocess isolation"
      min_lines: 150
    - path: "tests/test_diagnostic_metadata.py"
      provides: "AUDT-04 metadata schema validation tests"
      min_lines: 30
    - path: "tests/test_multi_seed_aggregation.py"
      provides: "Aggregation logic tests with mock CSV data"
      min_lines: 40
  key_links:
    - from: "scripts/studies/run_multi_seed_v13.py"
      to: "scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py"
      via: "subprocess.run with --noise-seed and --noise-percent CLI args"
      pattern: "subprocess\\.run.*Infer_BVMaster"
    - from: "scripts/studies/run_multi_seed_v13.py"
      to: "StudyResults/v14/multi_seed/"
      via: "CSV + PNG + JSON output"
      pattern: "StudyResults/v14/multi_seed"
---

<objective>
Create a multi-seed wrapper script that runs the v13 pipeline across 20 noise seeds at 2% noise, aggregates per-parameter recovery errors, and generates summary statistics + visualizations. Also create the shared AUDT-04 metadata test infrastructure.

Purpose: Establishes the v13 performance baseline (DIAG-01) that all later phases compare against, and creates the metadata validation tests reused by all Phase 7 plans.
Output: `scripts/studies/run_multi_seed_v13.py`, test files, outputs in `StudyResults/v14/multi_seed/`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-baseline-diagnostics/07-CONTEXT.md
@.planning/phases/07-baseline-diagnostics/07-RESEARCH.md

@scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py
@scripts/_bv_common.py

<interfaces>
<!-- Key interfaces the executor needs from the codebase -->

From scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py:
- CLI args: `--noise-seed INT`, `--noise-percent FLOAT`
- Output CSV: `StudyResults/master_inference_v13/master_comparison_v13.csv`
- CSV columns: phase, k0_1, k0_2, alpha_1, alpha_2, k0_1_err_pct, k0_2_err_pct, alpha_1_err_pct, alpha_2_err_pct, loss, time_s
- P2 row (final result): phase starts with "P2"
- Output base dir: `StudyResults/master_inference_v13/`

From scripts/_bv_common.py:
```python
K0_HAT_R1 = K0_PHYS_R1 / K_SCALE  # ~1.2632e-03
K0_HAT_R2 = K0_PHYS_R2 / K_SCALE  # ~5.2632e-05
ALPHA_R1 = 0.627
ALPHA_R2 = 0.5
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AUDT-04 metadata tests and multi-seed aggregation tests</name>
  <files>tests/test_diagnostic_metadata.py, tests/test_multi_seed_aggregation.py</files>
  <behavior>
    - Test: metadata JSON must contain required keys (tool_name, phase, requirement, justification_type, reference, rationale, parameters, generated)
    - Test: justification_type must be one of "literature", "empirical", "simplest"
    - Test: validate_metadata() returns True for valid metadata, False for missing keys
    - Test: aggregation of mock P2 results from 5 seeds computes correct median, IQR (25th/75th percentile), and max for each of k0_1_err_pct, k0_2_err_pct, alpha_1_err_pct, alpha_2_err_pct
    - Test: parse_v13_csv() extracts P2 row correctly from mock CSV data
    - Test: aggregation handles missing P2 rows gracefully (skip seed, log warning)
  </behavior>
  <action>
    Create `tests/test_diagnostic_metadata.py`:
    - Define REQUIRED_METADATA_KEYS as a module constant (tool_name, phase, requirement, justification_type, reference, rationale, parameters, generated)
    - Define VALID_JUSTIFICATION_TYPES = {"literature", "empirical", "simplest"}
    - Write a `validate_metadata(metadata: dict) -> bool` helper function that checks all required keys present and justification_type is valid
    - Write pytest tests: test_valid_metadata, test_missing_key, test_invalid_justification_type
    - Export validate_metadata for reuse by other test files

    Create `tests/test_multi_seed_aggregation.py`:
    - Create a pytest fixture that generates mock master_comparison_v13.csv content with 7 rows (S1-S4, P1, P2) and known error percentages for the P2 row
    - Write tests for parse_v13_csv(csv_path) that extracts P2 row as a dict with k0_1, k0_2, alpha_1, alpha_2 and their error percentages
    - Write tests for aggregate_seed_results(results: list[dict]) that computes median, p25, p75, max of error percentages using numpy
    - Use 5 mock seeds with known values so expected median/IQR/max can be computed by hand
    - Test graceful handling when P2 row is missing (returns None, caller skips)

    Run tests -- they should FAIL (RED) since implementation does not exist yet.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_diagnostic_metadata.py tests/test_multi_seed_aggregation.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>Test files exist, all tests fail with ImportError or NameError (RED phase), test logic is correct and expectations are verifiable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multi-seed wrapper script with aggregation and visualization</name>
  <files>scripts/studies/run_multi_seed_v13.py</files>
  <behavior>
    - parse_v13_csv(csv_path) extracts P2 row correctly
    - aggregate_seed_results(results) computes correct median/IQR/max
    - validate_metadata passes for the generated metadata.json
  </behavior>
  <action>
    Create `scripts/studies/run_multi_seed_v13.py` following codebase patterns (frozen dataclass config, `[tag]` print logging, argparse CLI).

    **Structure:**
    1. `MultiSeedConfig` frozen dataclass: num_seeds (default 20), noise_percent (default 2.0), seed_start (default 0), v13_script_path (auto-detected relative to this file), output_dir (default `StudyResults/v14/multi_seed`), timeout_per_seed (default 900s).

    2. `parse_v13_csv(csv_path: str) -> dict | None`: Read CSV, find row where phase starts with "P2", return dict with k0_1, k0_2, alpha_1, alpha_2 and their _err_pct values as floats. Return None if no P2 row found.

    3. `aggregate_seed_results(results: list[dict]) -> dict`: Given list of P2 result dicts, compute per-parameter: median, p25 (25th percentile), p75 (75th percentile), max of the _err_pct columns using numpy.percentile and numpy.max.

    4. `run_single_seed(seed: int, config: MultiSeedConfig) -> dict | None`: Run v13 via subprocess.run with `sys.executable`, `--noise-seed {seed}`, `--noise-percent {config.noise_percent}`. Capture stdout/stderr. Parse the output CSV from `StudyResults/master_inference_v13/master_comparison_v13.csv`. Return parsed P2 result dict or None on failure.

    5. `generate_plots(seed_results: list[dict], output_dir: str)`:
       - Box plot: one box per parameter (k0_1, k0_2, alpha_1, alpha_2) showing error percentages across seeds. Use matplotlib with clear axis labels. Save as `boxplot_errors.png`.
       - Per-seed scatter: x=seed index, y=error percentage, one subplot per parameter. Highlight outliers (>2x median). Save as `scatter_per_seed.png`.

    6. `write_summary_csv(seed_results, summary_stats, output_dir)`:
       - `seed_results.csv`: one row per seed with columns seed, k0_1, k0_2, alpha_1, alpha_2, k0_1_err_pct, k0_2_err_pct, alpha_1_err_pct, alpha_2_err_pct
       - `summary_statistics.csv`: one row per parameter with columns parameter, median_err_pct, p25_err_pct, p75_err_pct, max_err_pct

    7. `write_metadata(output_dir)`: Write `metadata.json` with AUDT-04 fields:
       - tool_name: "Multi-Seed Pipeline Robustness Assessment"
       - phase: "07-baseline-diagnostics"
       - requirement: "DIAG-01"
       - justification_type: "empirical"
       - reference: "Standard practice in inverse problems: test parameter recovery across noise realizations to assess estimator variance"
       - rationale: "Running across 20 noise seeds at 2% noise quantifies pipeline sensitivity to noise realization and identifies parameters with high variance"
       - parameters: {n_seeds, noise_percent, pipeline_version: "v13"}
       - generated: ISO timestamp

    8. `main()` with argparse: `--num-seeds` (default 20), `--noise-percent` (default 2.0), `--seed-start` (default 0), `--timeout` (default 900). Run seeds sequentially, collect results, compute stats, generate plots, write CSVs, write metadata.

    **Important:** The functions parse_v13_csv and aggregate_seed_results must be importable by the test files. Add the project root to sys.path in the test files using the same pattern as other test files in the project.

    After implementing, run the tests from Task 1 -- they should PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_diagnostic_metadata.py tests/test_multi_seed_aggregation.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass (GREEN). Script exists at scripts/studies/run_multi_seed_v13.py with all 8 components. parse_v13_csv correctly extracts P2 rows, aggregate_seed_results produces correct statistics, metadata.json passes AUDT-04 validation.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_diagnostic_metadata.py tests/test_multi_seed_aggregation.py -x -v` -- all tests pass
2. `python scripts/studies/run_multi_seed_v13.py --help` -- shows CLI args (num-seeds, noise-percent, seed-start, timeout)
3. `python -c "from scripts.studies.run_multi_seed_v13 import parse_v13_csv, aggregate_seed_results; print('imports OK')"` -- functions are importable
</verification>

<success_criteria>
- Multi-seed wrapper script exists with subprocess isolation per seed
- Summary statistics correctly compute median, IQR, worst-case per parameter
- Box plot and scatter plot generation code exists
- JSON metadata sidecar passes AUDT-04 schema validation
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-baseline-diagnostics/07-01-SUMMARY.md`
</output>
