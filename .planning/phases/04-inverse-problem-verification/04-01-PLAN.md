---
phase: 04-inverse-problem-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Forward/steady_state/common.py
  - tests/test_inverse_verification.py
  - StudyResults/inverse_verification/gradient_fd_convergence.json
autonomous: true
requirements: [INV-02]

must_haves:
  truths:
    - "add_percent_noise supports both legacy RMS mode and new multiplicative signal mode"
    - "Surrogate FD gradient converges at O(h^2) for central differences"
    - "All existing tests still pass after noise model update (backward compatible)"
  artifacts:
    - path: "Forward/steady_state/common.py"
      provides: "Updated add_percent_noise with mode parameter"
      contains: "mode.*signal"
    - path: "tests/test_inverse_verification.py"
      provides: "INV-02b surrogate FD convergence test + scaffold for remaining tests"
      contains: "TestSurrogateFDConvergence"
    - path: "StudyResults/inverse_verification/gradient_fd_convergence.json"
      provides: "FD convergence rate data for Phase 6 report"
  key_links:
    - from: "tests/test_inverse_verification.py"
      to: "Forward/steady_state/common.py"
      via: "import add_percent_noise with mode='signal'"
      pattern: "add_percent_noise.*mode.*signal"
    - from: "tests/test_inverse_verification.py"
      to: "Surrogate/objectives.py"
      via: "SurrogateObjective for FD gradient testing"
      pattern: "SurrogateObjective"
---

<objective>
Update the noise model for backward-compatible multiplicative noise and implement the fast surrogate FD gradient convergence test (INV-02b). This plan establishes the foundation for Plan 02's slow Firedrake tests.

Purpose: The noise model update is a prerequisite for all INV-01 parameter recovery tests. The surrogate FD convergence test (INV-02b) is fast (no Firedrake) and validates gradient machinery used by the optimizer.
Output: Updated `add_percent_noise()`, test file scaffold with working INV-02b test, FD convergence JSON artifact.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inverse-problem-verification/04-CONTEXT.md
@.planning/phases/04-inverse-problem-verification/04-RESEARCH.md
@Forward/steady_state/common.py
@Surrogate/objectives.py
@tests/conftest.py
@scripts/_bv_common.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From Forward/steady_state/common.py (current signature to extend):
```python
def add_percent_noise(
    values: Sequence[float], noise_percent: float, *, seed: int | None = None
) -> np.ndarray:
```

From Surrogate/objectives.py (for FD gradient testing):
```python
class SurrogateObjective:
    """Joint CD+PC surrogate objective with FD gradient."""
    def __call__(self, k0_1, k0_2, alpha_1, alpha_2) -> float: ...
    def gradient(self, k0_1, k0_2, alpha_1, alpha_2) -> np.ndarray: ...
```

From scripts/_bv_common.py (true parameter values):
```python
K0_HAT_R1 = ...  # true k0 for reaction 1
K0_HAT_R2 = ...  # true k0 for reaction 2
ALPHA_R1 = ...   # true alpha for reaction 1
ALPHA_R2 = ...   # true alpha for reaction 2
```

From tests/conftest.py:
```python
skip_without_firedrake  # decorator for Firedrake-dependent tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update add_percent_noise with backward-compatible multiplicative mode</name>
  <files>Forward/steady_state/common.py</files>
  <action>
Extend `add_percent_noise()` in `Forward/steady_state/common.py` (around line 136) to support a `mode` keyword argument:

1. Add `mode: str = "rms"` parameter to the function signature (default preserves backward compatibility).
2. When `mode="rms"` (default): keep existing behavior exactly (sigma = pct/100 * RMS(finite values), global sigma).
3. When `mode="signal"`: compute per-point multiplicative noise:
   - `sigma_i = (pct / 100.0) * |values_i|` for each point
   - Floor sigma at 1e-12 to avoid zero-sigma for near-zero values
   - Draw standard normal noise and multiply by per-point sigma
   - Only add noise to finite entries (preserve NaN handling)
4. Raise `ValueError` for unknown mode values.
5. Update the docstring to document both modes, explaining that "rms" is the legacy global-sigma mode and "signal" is per-point multiplicative noise where `noise_std = noise_percent/100 * |signal|`.

Do NOT change any other function in the file. The default `mode="rms"` ensures all 26+ existing callers are unaffected.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -c "
from Forward.steady_state.common import add_percent_noise
import numpy as np

# Test backward compat (rms mode, default)
v = np.array([1.0, 2.0, 3.0])
noisy_rms = add_percent_noise(v, 5.0, seed=42)
assert noisy_rms.shape == v.shape
assert not np.allclose(noisy_rms, v), 'RMS noise should change values'

# Test signal mode
noisy_sig = add_percent_noise(v, 5.0, seed=42, mode='signal')
assert noisy_sig.shape == v.shape
assert not np.allclose(noisy_sig, v), 'Signal noise should change values'

# Test 0% noise returns copy
zero = add_percent_noise(v, 0.0, mode='signal')
assert np.array_equal(zero, v)

# Test NaN preservation
v_nan = np.array([1.0, np.nan, 3.0])
noisy_nan = add_percent_noise(v_nan, 5.0, seed=42, mode='signal')
assert np.isnan(noisy_nan[1])
assert np.isfinite(noisy_nan[0]) and np.isfinite(noisy_nan[2])

# Test unknown mode raises
try:
    add_percent_noise(v, 5.0, mode='bad')
    assert False, 'Should have raised ValueError'
except ValueError:
    pass

print('All add_percent_noise tests passed')
"
    </automated>
  </verify>
  <done>add_percent_noise has mode="rms" (default, backward compatible) and mode="signal" (per-point multiplicative). Existing callers unaffected. NaN handling preserved in both modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_inverse_verification.py with surrogate FD convergence test (INV-02b)</name>
  <files>tests/test_inverse_verification.py, StudyResults/inverse_verification/gradient_fd_convergence.json</files>
  <behavior>
    - Test: Central FD gradient at step sizes h=1e-3, 1e-5, 1e-7 converges at O(h^2)
    - Test: FD gradient at h=1e-5 agrees with SurrogateObjective.gradient() within 1%
    - Test: Convergence rate (log-log slope) is approximately 2.0 (within [1.5, 2.5])
    - Artifact: JSON with per-step-size gradient values and computed convergence rate saved to StudyResults/inverse_verification/
  </behavior>
  <action>
Create `tests/test_inverse_verification.py` with:

1. Module-level constants: `_ROOT` (project root via `os.path.dirname`), `_OUTPUT_DIR = os.path.join(_ROOT, "StudyResults", "inverse_verification")`.

2. Module-scoped `nn_ensemble` fixture (reuse pattern from `test_surrogate_fidelity.py`): load the NN ensemble surrogate, build SurrogateObjective with true targets from `_bv_common`.

3. `class TestSurrogateFDConvergence` (no `@pytest.mark.slow`, no Firedrake needed):
   - `test_fd_convergence_rate`: Evaluate the SurrogateObjective at true parameters. For each of the 4 parameters, compute central FD gradient at h=1e-3, 1e-5, 1e-7. Use the finest step (1e-7) as reference. Compute convergence rate from log-log slope of |FD(h) - FD(h_ref)| vs h for the two coarser steps. Assert rate is in [1.5, 2.5] for each parameter (O(h^2) central differences). Also check that FD at h=1e-5 agrees with `SurrogateObjective.gradient()` within 1% component-wise.
   - Save JSON artifact to `StudyResults/inverse_verification/gradient_fd_convergence.json` containing: step_sizes, per-parameter FD values at each step, convergence rates, analytic gradient comparison.

4. Add placeholder class stubs (empty, with docstrings only) for Plan 02:
   - `class TestParameterRecovery` with docstring "INV-01: Implemented in Plan 02"
   - `class TestGradientConsistencyPDE` with docstring "INV-02a: Implemented in Plan 02"
   - `class TestMultistartBasin` with docstring "INV-03: Implemented in Plan 02"

Follow Phase 3's `test_surrogate_fidelity.py` patterns for fixture structure, output directory creation, and JSON artifact format.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_inverse_verification.py::TestSurrogateFDConvergence -x -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>TestSurrogateFDConvergence passes: central FD shows O(h^2) convergence, agrees with analytic gradient within 1%, JSON artifact saved to StudyResults/inverse_verification/gradient_fd_convergence.json. Placeholder stubs exist for Plan 02 classes.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_inverse_verification.py::TestSurrogateFDConvergence -x -v` passes
2. `python -c "from Forward.steady_state.common import add_percent_noise; print('import ok')"` succeeds
3. `python -m pytest tests/ -m "not slow" -x --ignore=tests/test_mms_convergence.py -q` -- existing fast tests still pass (backward compat)
4. `StudyResults/inverse_verification/gradient_fd_convergence.json` exists with convergence rate data
</verification>

<success_criteria>
- add_percent_noise supports mode="signal" for multiplicative noise, default mode="rms" preserves all existing behavior
- Surrogate FD convergence test demonstrates O(h^2) rate and agrees with analytic gradient within 1%
- JSON artifact saved for Phase 6 report consumption
- Test file scaffold ready for Plan 02 to implement remaining test classes
</success_criteria>

<output>
After completion, create `.planning/phases/04-inverse-problem-verification/04-01-SUMMARY.md`
</output>
