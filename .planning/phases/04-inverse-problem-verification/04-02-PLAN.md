---
phase: 04-inverse-problem-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_inverse_verification.py
  - tests/test_v13_verification.py
  - StudyResults/inverse_verification/parameter_recovery_summary.json
  - StudyResults/inverse_verification/parameter_recovery_details.csv
  - StudyResults/inverse_verification/gradient_pde_consistency.json
  - StudyResults/inverse_verification/multistart_basin.json
autonomous: true
requirements: [INV-01, INV-02, INV-03]

must_haves:
  truths:
    - "Parameter recovery test infers known parameters from PDE-generated synthetic data at 0%, 1%, 2%, 5% noise with 3 realizations each"
    - "Recovery errors pass noise-scaled soft gates (0%<5%, 1%<10%, 2%<15%, 5%<30% mean max relative error)"
    - "PDE gradient consistency test compares adjoint/FD gradients at 3 evaluation points within 1% tolerance"
    - "Multistart analysis shows >50% of 20 polished candidates recover parameters within 10%"
    - "Old Tests 1, 2, 7 removed from test_v13_verification.py"
  artifacts:
    - path: "tests/test_inverse_verification.py"
      provides: "Complete inverse verification test suite (INV-01, INV-02a, INV-03)"
      contains: "TestParameterRecovery"
    - path: "StudyResults/inverse_verification/parameter_recovery_summary.json"
      provides: "Recovery error statistics at each noise level"
    - path: "StudyResults/inverse_verification/parameter_recovery_details.csv"
      provides: "Per-run per-parameter recovery details"
    - path: "StudyResults/inverse_verification/gradient_pde_consistency.json"
      provides: "PDE adjoint vs FD gradient comparison"
    - path: "StudyResults/inverse_verification/multistart_basin.json"
      provides: "Multistart convergence basin statistics"
    - path: "tests/test_v13_verification.py"
      provides: "Cleaned file with Tests 1, 2, 7 removed"
  key_links:
    - from: "tests/test_inverse_verification.py"
      to: "scripts/_bv_common.py"
      via: "true parameter values and PDE target generation"
      pattern: "_generate_targets_with_pde|K0_HAT_R1"
    - from: "tests/test_inverse_verification.py"
      to: "Surrogate/multistart.py"
      via: "run_multistart_inference for INV-03"
      pattern: "run_multistart_inference"
    - from: "tests/test_inverse_verification.py"
      to: "Forward/steady_state/common.py"
      via: "add_percent_noise with mode='signal'"
      pattern: "add_percent_noise.*mode.*signal"
---

<objective>
Implement the three slow Firedrake-dependent inverse verification tests (INV-01 parameter recovery, INV-02a PDE gradient consistency, INV-03 multistart basin) and clean up superseded tests from the old test file.

Purpose: Prove the v13 inverse pipeline correctly recovers parameters from synthetic data, has consistent gradients, and converges from multiple starting points -- the core V&V evidence for the inverse problem layer.
Output: Complete test_inverse_verification.py with all INV requirements passing, JSON/CSV artifacts for Phase 6 report, cleaned test_v13_verification.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inverse-problem-verification/04-CONTEXT.md
@.planning/phases/04-inverse-problem-verification/04-RESEARCH.md
@.planning/phases/04-inverse-problem-verification/04-01-SUMMARY.md
@tests/test_inverse_verification.py
@tests/test_v13_verification.py
@scripts/_bv_common.py
@Surrogate/multistart.py
@Surrogate/objectives.py
@Inverse/objectives.py
@Inverse/inference_runner/__init__.py
@Forward/steady_state/common.py
@tests/conftest.py
@scripts/surrogate/Infer_BVMaster_charged_v13_ultimate.py

<interfaces>
<!-- Key contracts from Plan 01 output and existing code -->

From tests/test_inverse_verification.py (created by Plan 01):
```python
# Module-level constants
_ROOT = ...
_OUTPUT_DIR = os.path.join(_ROOT, "StudyResults", "inverse_verification")

# nn_ensemble fixture (module-scoped) -- loads NN ensemble surrogate
# TestSurrogateFDConvergence -- already implemented in Plan 01
# TestParameterRecovery -- stub to be replaced
# TestGradientConsistencyPDE -- stub to be replaced
# TestMultistartBasin -- stub to be replaced
```

From Forward/steady_state/common.py (updated by Plan 01):
```python
def add_percent_noise(
    values: Sequence[float], noise_percent: float, *,
    seed: int | None = None, mode: str = "rms"
) -> np.ndarray:
    # mode="signal": per-point multiplicative noise
```

From scripts/_bv_common.py:
```python
K0_HAT_R1, K0_HAT_R2, ALPHA_R1, ALPHA_R2  # true parameter values
_generate_targets_with_pde()  # cached PDE target generation
_compute_errors()  # error computation utility
make_bv_solver_params()  # solver parameter builder
```

From Surrogate/multistart.py:
```python
run_multistart_inference(surrogate, target_cd, target_pc, bounds_k0_1, bounds_k0_2, bounds_alpha, config, subset_idx)
MultiStartConfig(n_grid, n_top_candidates, polish_maxiter, ...)
# result.candidates: tuple of MultiStartCandidate (k0_1, k0_2, alpha_1, alpha_2, polished_loss)
```

From tests/test_v13_verification.py (classes to remove):
```python
class TestZeroNoiseIdentity  # Test 1 -- subsumed by INV-01 0% noise
class TestKnownParameterRoundtripPDE  # Test 2 -- subsumed by INV-01
class TestMultistartConvergenceBasin  # Test 7 -- subsumed by INV-03
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TestParameterRecovery (INV-01) -- parameter recovery at 4 noise levels</name>
  <files>tests/test_inverse_verification.py, StudyResults/inverse_verification/parameter_recovery_summary.json, StudyResults/inverse_verification/parameter_recovery_details.csv</files>
  <action>
Replace the `TestParameterRecovery` stub in `tests/test_inverse_verification.py` with the full implementation.

**Class: TestParameterRecovery** (marked `@pytest.mark.slow`, uses `@skip_without_firedrake`):

1. **Test method: `test_recovery_at_noise_levels`**
   - Generate clean PDE targets at true parameters using `_generate_targets_with_pde()` from `scripts/_bv_common.py` (or equivalent caching pattern). These are the ground-truth I-V curves.
   - For each noise level [0.0, 1.0, 2.0, 5.0]:
     - For each seed [42, 43, 44]:
       - Apply noise using `add_percent_noise(clean_targets, noise_pct, seed=seed, mode="signal")`
       - Run the full 7-phase v13 pipeline (S1-S5 surrogate + P1-P2 PDE refinement). Import stage functions from the v13 script rather than invoking CLI. If direct import is impractical, run the surrogate-phase inference via `run_multistart_inference()` for stages S1-S5, then PDE refinement stages P1-P2 using `run_inverse_inference()` from `Inverse/inference_runner/`.
       - Compute per-parameter relative error vs true values for all 4 parameters (k0_1, k0_2, alpha_1, alpha_2).
     - Compute mean and std of per-parameter max relative error across the 3 realizations.
   - **Soft gates on mean max relative error**: 0% < 5%, 1% < 10%, 2% < 15%, 5% < 30%.
   - **Artifacts**:
     - `parameter_recovery_summary.json`: timestamp, noise_levels, realizations_per_level, per-noise-level mean/std of max relative error, per-parameter mean/std errors, pass/fail per gate.
     - `parameter_recovery_details.csv`: columns [noise_pct, seed, param_name, true_value, recovered_value, relative_error_pct].

**IMPORTANT notes from RESEARCH.md:**
- Always use PDE-generated targets, never surrogate-generated (avoid inverse crime).
- If the full 7-phase pipeline import is complex, the test can run surrogate-only inference (S1-S5 via multistart) as a pragmatic fallback, but document this deviation. The user prefers the full pipeline.
- If `run_multistart_inference()` segfaults (PyTorch/MPI issue per Pitfall 1), fall back to loop-based evaluation as in existing Test 7. Document the fallback.
- Use `mode="signal"` for noise (per CONTEXT.md locked decision).
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_inverse_verification.py::TestParameterRecovery -m slow --tb=short -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>Parameter recovery test passes at all 4 noise levels with 3 realizations each. Mean max relative errors within soft gates. JSON summary and CSV details saved to StudyResults/inverse_verification/.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TestGradientConsistencyPDE (INV-02a) and TestMultistartBasin (INV-03)</name>
  <files>tests/test_inverse_verification.py, StudyResults/inverse_verification/gradient_pde_consistency.json, StudyResults/inverse_verification/multistart_basin.json</files>
  <action>
Replace the `TestGradientConsistencyPDE` and `TestMultistartBasin` stubs in `tests/test_inverse_verification.py`.

**Class: TestGradientConsistencyPDE** (marked `@pytest.mark.slow`, `@skip_without_firedrake`):

1. **Test method: `test_pde_adjoint_vs_fd`**
   - Evaluate at 3 parameter points: (a) true parameters, (b) +10% perturbation of all params, (c) -10% perturbation.
   - At each point:
     - Build a ReducedFunctional using `build_reduced_functional()` from `Inverse/inference_runner/` (or the equivalent PDE objective setup from the v13 pipeline).
     - Get adjoint gradient via `rf.derivative()`.
     - Compute central FD gradient with h ~ 1e-5 (sqrt(machine_eps) range, above PDE solver tolerance).
     - Assert component-wise relative agreement within 1%.
   - **IMPORTANT from RESEARCH open question 1**: The BV I-V curve objective may NOT be adjoint-differentiable through multi-voltage-point warm-started solves. If `build_reduced_functional()` does not work for the BV objective, fall back to testing at the single-solve PDE level (similar to existing Test 2 structure) OR test FD convergence rate on the PDE objective (FD at multiple step sizes showing O(h^2)). Document whichever approach works.
   - Save JSON artifact: `gradient_pde_consistency.json` with evaluation points, adjoint gradient, FD gradient, relative errors, pass/fail.

**Class: TestMultistartBasin** (marked `@pytest.mark.slow`):

1. **Test method: `test_multistart_convergence`**
   - Generate clean PDE targets at true parameters (reuse fixture/cache from TestParameterRecovery if available, or generate independently).
   - Run `run_multistart_inference()` with production config: `MultiStartConfig(n_grid=20_000, n_top_candidates=20, polish_maxiter=60, ...)`.
   - If `run_multistart_inference()` segfaults (Pitfall 1), fall back to manual loop: evaluate grid via loop-based `predict()`, sort, polish top-20 with L-BFGS-B individually. Document fallback.
   - Compute 4 statistics from result:
     1. `pct_converged`: % of 20 polished candidates with all 4 params within 10% of truth
     2. `loss_distribution`: min, median, max of polished losses
     3. `parameter_spread`: std dev of recovered params across converged candidates
     4. `best_worst_gap`: ratio of worst to best polished loss
   - **Soft gate**: pct_converged > 50%
   - Save JSON artifact: `multistart_basin.json` with all 4 statistics, per-candidate details, pass/fail.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_inverse_verification.py::TestGradientConsistencyPDE tests/test_inverse_verification.py::TestMultistartBasin -m slow --tb=short -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>PDE gradient test passes (adjoint vs FD within 1% at 3 points, or documented fallback). Multistart test passes (>50% convergence). JSON artifacts saved for both.</done>
</task>

<task type="auto">
  <name>Task 3: Remove superseded Tests 1, 2, 7 from test_v13_verification.py</name>
  <files>tests/test_v13_verification.py</files>
  <action>
Remove the three test classes from `tests/test_v13_verification.py` that are now subsumed by `test_inverse_verification.py`:

1. Remove `class TestZeroNoiseIdentity` (Test 1) -- subsumed by INV-01 at 0% noise
2. Remove `class TestKnownParameterRoundtripPDE` (Test 2) -- subsumed by INV-01 full pipeline
3. Remove `class TestMultistartConvergenceBasin` (Test 7) -- subsumed by INV-03

Also remove any fixtures that were ONLY used by these three classes (check before removing). Keep all other tests (Tests 3, 4, 5/6 -- TestGradientVerification, TestObservableSignAndScale, TestSensitivityMonotonicity) and their shared fixtures intact.

Verify the remaining tests in test_v13_verification.py still import correctly and pass.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/ResearchForwardSolverClone/FireDrakeEnvCG/PNPInverse && python -m pytest tests/test_v13_verification.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <done>Tests 1, 2, 7 removed from test_v13_verification.py. Remaining tests (3, 4, 6) collect and import correctly. No orphaned fixtures.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_inverse_verification.py -m slow --tb=short -v` -- all 4 test classes pass (FD convergence, parameter recovery, PDE gradient, multistart)
2. `python -m pytest tests/test_v13_verification.py --collect-only -q` -- only Tests 3, 4, 6 remain
3. `ls StudyResults/inverse_verification/` -- contains parameter_recovery_summary.json, parameter_recovery_details.csv, gradient_fd_convergence.json, gradient_pde_consistency.json, multistart_basin.json
4. `python -m pytest tests/ -m "not slow" -x --ignore=tests/test_mms_convergence.py -q` -- fast test suite still passes
</verification>

<success_criteria>
- INV-01: Parameter recovery passes soft gates at all 4 noise levels (mean across 3 realizations)
- INV-02a: PDE gradient test passes (adjoint vs FD within 1%, or documented FD-only fallback)
- INV-03: Multistart test shows >50% convergence to true minimum from 20 polished candidates
- All JSON/CSV artifacts saved to StudyResults/inverse_verification/ for Phase 6 consumption
- Tests 1, 2, 7 removed from test_v13_verification.py without breaking remaining tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-inverse-problem-verification/04-02-SUMMARY.md`
</output>
