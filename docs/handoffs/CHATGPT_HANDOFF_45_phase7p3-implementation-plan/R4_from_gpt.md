Approved. The protocol is now internally consistent: pH 2 is genuinely held out, A is treated as a frame/gauge issue, C is tested against equal-complexity alternatives, and the pH-4 under-constraint is explicitly acknowledged instead of hidden.

One nonblocking operational note: before opening pH 2, freeze an artifact containing the selected model family, parameter ranges/brackets, scorer, tolerances, and plots from pH 4/6.39. That makes the “no iteration afterward” rule auditable.

VERDICT: APPROVED